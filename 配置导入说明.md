# 物品配置导入样品

## 用途与范围

CSV批量填写配置 → 编辑器工具检查并按ID找到已有ItemData → 更新资产字段 → 现有拾取、背包、使用逻辑继续读取资产。

这不是玩家存档，不保存当前持有数量；不是运行时加载或热更新。第一版只更新已有物品，不创建新资产，不改场景和Prefab引用。水和绷带样例来自现有资产，默认数值不变。

## 文件与字段

- 表：`Assets/_Project/Data/Config/Items.csv`。
- 菜单脚本：`Assets/_Project/Editor/ItemConfigImporter.cs`。
- 表格解析：`Assets/_Project/Editor/ItemConfigCsv.cs`。

| 列名 | 含义 | 样例 |
|---|---|---|
| itemId | 已有资产的稳定ID，不随意改名 | water / bandage |
| displayName | 显示名称 | 矿泉水 / 绷带 |
| itemType | 枚举名称 | Water / Medical / Quest / Other |
| maxStack | 单槽堆叠上限，整数且至少1 | 3 |
| effectAmount | 补水或治疗效果，非负有限数值 | 25 / 15 |

使用UTF-8逗号分隔CSV，首行列名及顺序保持原样；小数用英文点。支持带引号的逗号和双引号转义，不支持单元格内换行。可以用文本编辑器修改，WPS/Excel保存时选择CSV UTF-8而非xlsx格式改后缀。

## 第一次验证

1. 停止Play，打开CSV，只把water的effectAmount从25改成40并保存。
2. 等Unity编译完成，点击`Tools > Evacuation Test > Import Item Config CSV`。
3. Console应出现`Imported 2 item configurations`；查看原`Item_Water`资产，Effect Amount应为40，已有引用不需要重拖。
4. Play后让饮水值至少缺40，使用一瓶水，应按40恢复；接近上限时按实际缺口恢复。绷带配置保持15。
5. 如需恢复原平衡，把CSV中的40改回25，再运行一次导入。表格后续会覆盖对应资产字段，建议以CSV作为这两种物品的配置来源。

整张表先解析并检查，再检查所有ID能否映射到资产；重复ID、未知ID和非法数值会在写入前停止。通过Undo登记修改，且只保存涉及的资产。没有实现任意磁盘写入失败后的事务回滚，不把预检查说成万能原子性。

## 验证记录

- 已完成：编辑器工具使用Unity安装中的编译器与程序集做独立编译检查；10项CSV解析测试通过（样例、引号/小数、重复ID、非法上限、负数、NaN、未知类型、不闭合引号、空表、表头）；CSV数据预览已检查。
- 2026-09-10用户确认菜单导入、资产字段更新与Play效果均成功；提供Console截图显示`Imported 2 item configurations`。记录为该样品已验证，不代表最新Release已重新构建，也不推断用户是否将测试数值恢复成25。

## 菜单点击到保存的调用链

`Editor`目录中的静态方法加`[MenuItem("Tools/Evacuation Test/Import Item Config CSV")]`，Unity编译后注册菜单，点击调用`Import()`，不需要挂到GameObject。路径中斜杠表示菜单层级，不是磁盘目录。

`CsvPath`是固定的项目文件路径；`File.ReadAllText`读取UTF-8文本，自定义`ItemConfigCsv.Parse`按列转换成`List<ItemConfigRow>`并校验。不是JsonUtility，也不是点击后弹出文件选择框。

`AssetDatabase.FindAssets`在Items目录找ItemData的GUID，转成资产路径并加载已有引用，再以物品业务ID建立字典；按每行ID匹配目标资产。业务ID（water）不等于Unity资产GUID。

`SerializedObject`是编辑器修改序列化字段的入口，`FindProperty("effectAmount").floatValue`对应原资产字段，`ApplyModifiedProperties`应用修改，`SaveAssetIfDirty`保存资产，Undo记录支持撤销。本次没有生成新资产，也不是把资产转换成JSON。构建后的游戏只读取已更新的资产，不运行这个Editor导入工具。

## 复述与自测

自己的话：我在CSV中批量配置物品，编辑器导入工具根据ID更新已有数据资产，玩法继续引用资产，所以不需要修改背包代码或重新绑定物品。

1. CSV和玩家JSON存档分别保存什么？
2. 为什么导入更新原资产，不把它删除后重建？
3. 修改CSV后没执行导入，为什么游戏仍可能使用旧数值？
