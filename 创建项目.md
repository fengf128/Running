# 《撤离测试场》创建与开发记录

> 只记录已经真实完成并经过核对的内容。规划中、尚未实现或未经验证的功能，不视为项目成果，也不能提前写入简历。

## 文档分工

- `创建项目.md`：进度、已完成/未完成、Bug 与证据路径。
- `知识.md`：概念、区别、原理和设计原因。
- `操作与作用.md`：实际步骤、为什么操作、产生的作用和验证结果。

## 项目基本信息

- 项目暂用名称：`Running`
- 项目路径：`E:\Unity3D\Mission\Running`
- 创建日期：2026-08-19
- Unity 版本：2022.3.57f1c1
- 项目模板：Universal 3D（空白核心模板）
- 渲染管线：URP 14.0.11
- 当前初始场景：`Assets/Scenes/SampleScene.unity`
- 项目目标：《撤离测试场》俯视角 3D 生存撤离 Gameplay Demo

## 当前真实状态

- 已重新创建干净的 Universal 3D 项目。
- 已确认上一个 URP Sample 项目的 Terminal、Garden、Oasis、Cockpit、Benchmark、XR 和示例控制器等内容没有残留。
- 初始场景目前只有 Main Camera、Directional Light 和 Global Volume。
- 已安装 Input System 1.11.2，manifest、lock 文件和 Unity 编译日志均已验证。
- Active Input Handling 已从 `Both` 切换为 `Input System Package (New)`；项目代码不使用旧 Input Manager API。
- 已移除项目中的 Unity Online Services 启动包与 Unity Version Control（Plastic）集成，原配置已备份到项目外。
- 已初始化独立 Git 仓库，默认分支为 `main`。
- 已创建首个本地提交：`80cf9f2 Unity项目基线`；检查确认没有误跟踪 Library、Temp、Logs、UserSettings、IDE 工程文件或 Plastic 元数据。
- 已添加 Unity 专用 `.gitignore`，Library、Temp、Logs、UserSettings、IDE 工程文件和构建产物不会进入版本库。
- 已在清理后重新打开 Unity：Plastic/UOS 没有被重新生成，Editor.log 未发现脚本编译错误。
- 尚未编写《撤离测试场》的 Gameplay 代码。

## 已观察到的问题

### 编辑器布局警告

警告内容：

`The layout ... default-2022.dwlt could not be fully loaded`

原因：Unity 保存的全局编辑器布局包含当前项目不可用的 EditorWindow。该警告与游戏场景、脚本和 URP 配置无关，通常不影响项目运行。

只读检查确认，`current/default-2022.dwlt` 中仍保存着上一个 Sample 项目的 `TutorialWindow`、`TutorialLayout`、Unity Version Control 和 Cinemachine 窗口引用，因此在当前空白项目中无法完整恢复。之后尝试加载 `default/Default.wlt` 时也出现了同类提示。

当前安全处理方式：

1. 在 Console 中点击 `Clear`，先清除已经记录的旧警告；
2. 点击右上角 `Layout`，选择 `Tall` 或 `Wide`，暂时不要再次选择 `Default`；
3. 如果界面正常且清空后没有生成新警告，将当前布局另存为 `ExtractionDev`；
4. 只有警告在每次启动时重新产生，才需要在关闭 Unity 后备份并重置对应布局文件。执行前应确认精确目标，不直接删除其他 Unity 用户配置。

### Unity Version Control 工作区重复报错

报错内容：

`路径 E:\Unity3D\Mission\Running 已包含在某个工作区中`

只读检查确认，项目根目录的 `.plastic` 已登记为名为 `Running` 的 Unity Version Control（Plastic）云端工作区，同时项目安装了 `com.unity.collab-proxy`，Unity Hub/Collab 又尝试把相同路径加入工作区，因此产生重复绑定报错。

该问题属于版本控制配置，不影响 URP 场景、Gameplay 代码或 C# 编译。2026-08-19 已在关闭 Unity/Hub 后完成清理：

- 将 `.plastic`、`ignore.conf`、UOS 生成脚本及修改前配置移到 `E:\Unity3D\Mission\Running-setup-backup\2026-08-19-plastic-uos`；
- 从 `Packages/manifest.json` 和 `packages-lock.json` 移除 UOS、Plastic 及仅由 UOS 引入的 Newtonsoft JSON 条目；
- 移除各平台的 `UNITY_UOS_SECURITY` 脚本宏；
- 初始化项目自己的 Git 仓库。

清理采用可恢复备份，没有直接丢弃原 Plastic 工作区信息。`EditorSettings.asset` 中仍有 Unity 通用的 `m_CollabEditorSettings` 序列化字段，它不是已安装的 Plastic 包，无需手动破坏 Unity 设置文件。

重新打开项目后的 Editor.log 仅记录一次 `EditorUpdateCheck: Failed ... 404`，属于编辑器更新检查失败，不是项目包、场景或脚本编译错误，当前无需围绕它修改项目。

### C# Code Optimization 模式

编辑器右下角提示当前为 `Mode: Debug`，并提供 `Switch to release mode`。开发阶段应保持 Debug，便于断点和调试；当前不切换 Release。最终性能验证或正式构建时再单独评估 Release，不能把编辑器调试模式切换等同于 Player 构建优化。

## 开发日志

### 2026-08-19｜重建空白 URP 项目

已完成：

- 使用 Unity 2022.3.57f1c1 创建 Universal 3D 空白项目。
- 确认项目使用 URP 14.0.11。
- 确认初始场景与 Assets 目录中没有旧 Sample 项目残留。
- 识别编辑器布局警告的原因与安全处理方式。
- 只读定位到旧 Sample 项目的 Tutorial/Cinemachine 等窗口引用仍存在于 Unity 全局布局缓存中，确认它不属于新项目资源。
- 定位到 Plastic 工作区重复绑定报错的原因，并确认当前保持 C# Debug 模式。
- 完整备份并移除 Plastic/UOS 项目集成，验证 manifest 与 lock 文件仍为有效 JSON，项目目录中不再存在 Plastic 工作区或 UOS 脚本。
- 添加 Unity `.gitignore`，初始化独立 `main` 分支 Git 仓库；等待 Unity 首次刷新后再做初始提交。
- 清理后重新打开 Unity 完成环境验收：未重新出现 Plastic/UOS，未发现脚本编译错误。
- 通过 Package Manager 安装 Input System 1.11.2；安装后首次核对 Active Input Handling 为 Both，并明确后续仅使用新版 Input System API。
- 将 Active Input Handling 从 Both 切换为 Input System Package (New)，配置值已核对为 `activeInputHandler: 1`。
- 本人通过终端完成 `git status → git add . → git commit → git log` 流程，并创建首个项目基线提交 `80cf9f2`。
- 重新建立开发记录。

尚未完成：

- 尚未建立正式游戏目录与主场景。
- Git 尚未配置远程仓库。
- 尚未实现玩家移动、瞄准、相机或其他 Gameplay 功能。
- 尚未产生可演示 MVP、Profiler 数据或可写入简历的成果。

## 后续记录规范

以后每个模块至少记录：

- 实际完成的功能与验证方法；
- 对应的 Unity 知识；
- 对应的 C# 知识；
- 选择该设计的原因；
- 本人能够独立解释和修改的代码；
- Bug、排查过程与最终结果；
- 截图、构建程序、视频、Profiler、README 或 Git 证据路径。
