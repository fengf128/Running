# 通过项目理解

> 只记录学习方法、当前理解和复习检查点；系统职责与完整功能链见 `思路.md`。

## 1. 每个模块的学习顺序

1. 需求：先写正常结果、失败情况和范围限制。
2. 朴素方案：说明最直接做法，以及它会产生的问题。
3. 应用场景：说明什么情况下使用该技术，什么情况下简单方案已足够。
4. 职责拆分：每个类只回答一个业务问题。
5. 设计选择：对比朴素方案、当前方案、优点、代价和扩展性。
6. 执行流程：从输入开始，跟到最终状态变化。
7. 代码对应：每段代码必须对应前面的需求或设计选择。
8. 边界验证：测试成功、失败、上限、空引用等情况。
9. 独立复述：能脱离代码讲清职责、原因、取舍和验证结果后才进入下一模块。

## 2. 讲解固定结构

### 整体

- 要解决什么问题。
- 数据从哪里来、流向哪里。
- 哪个对象负责选择、调用、处理和保存结果。

### 局部

- 类和接口的职责。
- 字段、参数、返回值分别代表什么。
- Unity 生命周期中的调用时机。
- 新增组件的用途、关键 Inspector 属性、配置效果和错误配置结果。
- 使用的 C# 基础及其实际作用。

### 设计

- 为什么采用当前方案。
- 什么时候适用，什么时候直接调用或简单变量已经足够。
- 不这样做时会出现什么问题。
- 当前方案的优点、代价和适用范围。
- 面试时如何用自己的话说明。

## 3. 理解完成标准

- 能根据需求自己画出调用流程。
- 能说清每个类为什么存在，而不只会念代码。
- 能区分类型、实例、引用、接口、参数和返回值。
- 能说明至少一个替代方案及当前选择的理由。
- 能预测正常情况和边界情况的运行结果。
- 能修改一个参数或规则，并判断需要改哪些代码。

## 4. 当前学习检查点：物品与背包

暂不继续增加功能，先能独立回答：

- E 键、PlayerInteractor、IInteractable 各负责什么。
- `InteractionName`、目标对象和 `interactor` 的区别。
- `closestInteractable` 在哪里取得和保存实际组件引用。
- 为什么接口中声明一次 Interact，实现类还要写具体方法体。
- ItemData、WorldItemPickup、PlayerInventory、InventorySlot 各负责什么。
- 为什么同一种物品共享 ItemData 引用。
- 为什么六格背包使用数组，槽位使用 class 实例。
- 为什么 TryAdd 先堆叠、再找空格，并返回 bool。
- 为什么确认添加成功后，世界物品才能消失。

## 5. 当前核心阅读规则

### 对象.方法（实参）

- 点号左边是接收调用、实际执行行为的对象。
- `slots[i].TryFill(itemData)` 中，`slots[i]` 已选中第 i 个槽位实例；TryFill 只修改该实例的 item 和 quantity。
- InventorySlot 不需要知道自己的数组下标；PlayerInventory 的循环变量 i 负责选择槽位和记录编号。
- 括号里面是调用方传给方法的信息。
- 方法声明中的参数名是形参；调用时传入的具体值是实参。
- `closestInteractable.Interact(gameObject)` 中：目标执行 Interact，Player 作为实参传入。
- 进入 `Interact(GameObject interactor)` 后，形参 interactor 保存 Player 引用。
- Input Action 只决定何时调用；目标来自物理检测，Player 来自 PlayerInteractor 所在 GameObject。

### 类型、实例与接口引用

- 类型规定对象能保存什么、能做什么；实例保存具体运行状态。
- 接口类型变量可保存任何实现该接口的实例引用。
- 变量声明为 IInteractable，不会改变实际对象仍是 WorldItemPickup 或 Door。
- 调用接口方法时，运行时执行实际对象提供的实现。
- 接口只声明共同功能；WorldItemPickup 负责写出拾取功能的方法体，并可拥有额外的 itemData。
- `IInteractable interactable = hitCollider.GetComponentInParent<IInteractable>()` 取得当前目标的实际组件引用。
- 当前目标更近时，`closestInteractable = interactable` 把该引用保存为最终候选。
- `closestInteractable?.Interact(gameObject)` 等于：目标不为空时，调用最近目标自己的 Interact，并传入 Player。

### 按能力查找

- 子弹不先问“是不是某种敌人”，而是查找 Health；有 Health 才具有受伤能力。
- PlayerInteractor 不先问“是不是 WorldItemPickup”，而是查找 IInteractable；实现接口才具有交互能力。
- 当前最近目标是矿泉水时，接口引用实际指向它的 WorldItemPickup；以后也可以指向 Door 等实现组件。
- 这种组合方式减少 Tag 和具体类型分支，新对象只需获得相应能力。

### 数组与实例

- `int[]` 的元素直接保存整数值。
- `InventorySlot[]` 的元素保存 InventorySlot 引用，创建数组后初始为 null。
- `new InventorySlot()` 创建一个独立槽位实例；六次 new 得到六份独立状态。
- 也能用平行数组保存物品和数量，但更容易失去同步；InventorySlot 把相关数据和规则放在一起。
- 使用物品时，PlayerInventory 根据 slotIndex 选中 `slots[slotIndex]`；该 InventorySlot 只负责减少自己的数量。
- 数量归零后同时清除 item，避免出现“有物品引用但数量为零”的残留状态。

### 使用物品

- PlayerController 只把数字键转换为槽位下标；PlayerItemUser 判断效果；PlayerInventory 选择并消耗槽位。
- `TryGetItem(slotIndex, out item)` 先取得 ItemData；效果成功后才调用 TryConsume。
- TryConsume 主动修改数据；返回 bool 报告结果；InventoryChanged.Invoke 再通知 UI 刷新。
- `inventory`、`health`、`hydration`、`item`、`used` 首次讲解时对应背包、生命、饮水、物品和是否成功。
- 新增另一种水或药时复用现有代码，只创建并配置新的 ItemData；新增全新效果时才扩展行为代码。

### 查询与修改

- `HasItem(item)` 回答“有没有”，只读槽位并返回 bool。
- `TryConsume(index)` 回答“能否消耗”，成功时会修改数量并通知 UI。
- 撤离点当前只需要查询任务物，因此调用 HasItem，不调用 TryConsume。

### 封装

- private 数据由所属类保管，外部通过 public 方法提出请求。
- 每个 public 方法代表一种允许的操作；方法内部集中检查规则并决定是否修改。
- 外部只需知道调用什么和返回什么，不需要知道数组怎样遍历、数量怎样归零。
- 辅助逻辑没有外部用途时继续保持 private，避免公开过多入口。

### 撤离结算

- `ExtractionZone`负责撤离条件和 completed；`ExtractionResultUI`只负责读取结果、显示和暂停。
- 当前是一对一明确调用，所以直接保存 UI 引用；多个独立系统都要响应撤离时再使用事件。
- Inspector 没绑定 `resultUI`时字段为 null，空引用检查会阻止无界面的撤离完成。
- Button的`On Click()`虽然已绑定方法，但EventSystem缺少Point或Left Click时仍不会触发。
- `RestartGame`先恢复timeScale，再通过SceneManager重载已加入Build Settings的当前场景。

## 6. ScriptableObject 与 ItemData

- `Assets/_Project/Data/Items`：普通文件夹，只负责资源分类。
- `ScriptableObject`：Unity 提供的可保存为 `.asset` 的数据对象基类。
- `ItemData.cs`：继承 `ScriptableObject` 的物品数据类型，规定物品有哪些字段。
- `Create → Evacuation Test → Item Data`：`CreateAssetMenu` 注册的菜单入口，不是额外安装的包。
- `Item_Water.asset`：一个具体的 `ItemData` 资产，保存矿泉水的实际配置。
- `WorldItemPickup` 和 `InventorySlot` 保存对该资产的引用，不重复复制物品定义。
- `ItemData item` 引用整份资产；`ItemType` 只是资产内部的一个枚举字段。
- `item = itemToAdd` 传递同一资产引用，因此之后可读取名称、类型、堆叠上限和效果值。
- 数量属于每个 `InventorySlot` 的运行时状态，不能写进共享的 `ItemData`。

## 7. 阶段复习易错点

### 职责拆分

- `PlayerController` 读输入，`PlayerShooter` 管射击规则，`ProjectilePool` 管复用，`Projectile` 管自身生命周期，`Health` 管伤害。
- 职责拆分解决代码耦合；对象池解决重复创建、销毁和 GC 压力，两者不是同一问题。

### 子弹状态

- `SetActive(true)` 启用 GameObject，使 Unity 开始调用 `FixedUpdate`。
- `isLaunched` 是自定义业务状态，只决定是否执行移动逻辑。
- 真正产生位移的是 `Rigidbody.MovePosition`。

### 交互来源

- 当前使用 `OverlapSphereNonAlloc` 球形重叠检测，不是射线。
- 目标来自范围检测；传入的 `gameObject` 是 `PlayerInteractor` 所在的 Player。
- `this` 表示当前 `PlayerInteractor` 组件，`gameObject` 表示它所在的 Player 对象。
- WorldItemPickup 不会引用 closestInteractable；引用方向是 closestInteractable 指向最近的 WorldItemPickup 实例。

### 共享定义与独立状态

- `ItemData` 保存同类物品共享的定义；技术上可修改，但不保存单个槽位的数量。
- `InventorySlot` 保存 `ItemData` 引用和独立 `quantity`，不复制名称、类型等定义。
- 若把数量放进共享 `ItemData`，所有引用该资产的槽位和世界物品会共享错误的数量。

## 8. 背包 UI 与事件

### 引用关系

- `InventoryUI.inventory` 引用 Player 上已有的 `PlayerInventory` 组件。
- `InventoryUI.slotViews` 是六个 `InventorySlotView` 组件引用组成的数组；数组不创建这些组件。
- `InventorySlotView.contentText` 再引用本格的 TMP 文字组件。
- 数据对应关系：`inventory.Slots[i] → slotViews[i] → contentText`。
- `inventory.Slots[0]` 是第一格数据；`slotViews[0]` 是第一格的显示组件。
- InventoryUI 负责按索引分配数据；InventorySlotView 只负责把收到的一格数据写入自己的文字。

### 声明类型与实际类型

- `IReadOnlyList<InventorySlot> slots = inventory.Slots` 不创建或复制集合。
- 局部变量按只读接口使用同一个 `InventorySlot[]` 数组对象。
- 声明类型限制当前代码能调用的成员；实际类型说明运行时对象究竟是什么。

### 事件链

- `InventoryChanged` 是项目自定义事件名，不是库函数，也不会自动监测数据。
- `+= Refresh` 保存目标 `InventoryUI` 实例与方法引用；不立即执行。
- `InventoryChanged?.Invoke()` 同步调用当前所有订阅方法。
- `Refresh()` 立即读取当前状态；重新启用 UI 时可补回停用期间未显示的变化。

### 两级刷新

- `InventoryUI.Refresh()` 负责读取集合、对应索引并协调六格。
- `InventorySlotView.Refresh(...)` 只负责把一个槽位转换为文字。
- for 循环第 i 轮执行 `slots[i] → slotViews[i].Refresh(...)`；每轮传一格，六轮合起来刷新六格。
- 单个标签可直接修改；重复列表更适合“列表协调者 + 单项 View”。
