# 通过项目理解

> 只用于复习和检验能否独立说明；答案与原理见`知识.md`，系统链见`思路.md`。

## 1. 固定学习方式

每个新功能按以下顺序：

1. 用一句话写“我想实现什么”。
2. 找出需要的数据、能力及其拥有者。
3. 按职责确定代码放在哪个脚本。
4. 选择GetComponent、Inspector引用、直接调用、返回值或事件。
5. 从输入跟到最终状态和UI。
6. 验证成功、失败、上限、空引用和重复触发。
7. 能说清原因、替代方案和取舍后再继续。

每个新概念按以下顺序复习：

1. 先用大白话说“它是什么、解决什么问题”。
2. 再把大白话对应到一行真实代码和一次运行结果。
3. 从请求者、执行者、数据变化、通知者和显示者串成逻辑链。
4. 由学习者先用自己的话复述，允许不专业；再只修正关键错误，不要求背标准答案。
5. 用一至三个小问题检查能否迁移到相似场景，并顺带复习其中的C#概念。

状态UI记忆口令：`先过门卫，再写白板`。整数比较像门卫，不变就不让更新进入；TMP缓冲区像可擦写白板，变化时在原有空间改字符。字符串插值则像每次重新拿一张纸写完整文字。

讲代码时固定回答：

- 这个类只负责什么？
- 字段引用谁，参数由谁传入，返回值告诉谁？
- Unity在什么时候调用？
- 为什么不把代码放在另一个脚本？
- Inspector漏配或边界条件出现时会怎样？

## 2. 理解完成标准

- 不看代码也能画出核心调用链。
- 能说清每个类为什么存在，而不是只念方法名。
- 能区分类型、实例、引用、接口、参数和返回值。
- 能根据需求找到数据拥有者和代码位置。
- 能说明一个替代方案，以及当前方案的优点和代价。
- 能预测成功、失败和边界情况的运行结果。
- 能修改一个规则，并判断需要改哪些脚本。

## 3. C#与Unity基础复习

### 类型、实例和引用

- `new PlayerInputActions()`创建了什么？变量保存的是什么？
- `InventorySlot[]`创建后，为什么每个元素还需要`new InventorySlot()`？
- `new InventorySlotSaveData[6]`创建了几个数组实例、几个槽位数据实例？
- `GetComponent<Health>()`是创建Health，还是取得已有实例的引用？
- 声明类型为`IInteractable`时，实际对象为什么仍可为WorldItemPickup？
- `IReadOnlyList<InventorySlot>`接收数组时，有没有复制第二份集合？

### 对象与组件

- `GetComponent<T>()`到底在哪个GameObject上查找？
- 为什么`player.GetComponent<Health>()`能找到，而撤离点直接`GetComponent<ExtractionResultUI>()`找不到？
- Inspector把Canvas拖入`ExtractionResultUI`字段时，保存的是GameObject、脚本文件还是组件实例？
- 固定跨对象依赖和运行时目标，分别更适合Inspector引用还是GetComponent？

### 调用与封装

- `对象.方法(实参)`中，点号左边和括号分别表示什么？
- private数据为什么仍需要public方法？
- public方法代表允许的操作，方法体中的规则又是什么？
- HasItem、TryAdd和TryConsume中，哪些只查询，哪些会修改？

### 直接调用与事件

- `messageUI.Show()`为什么是直接调用而不是事件？
- `InventoryChanged += Refresh`是否会立刻执行Refresh？
- `InventoryChanged?.Invoke()`通知了什么，为什么不直接携带数组？
- 撤离成功增加音效、存档和成就后，什么时候值得改成事件？

### Unity生命周期

- 首次启用时Awake、OnEnable和Start分别负责什么，先后顺序是什么？
- 对象停用再启用时，为什么只重复OnEnable而不重复Awake和Start？
- FixedUpdate为什么适合Rigidbody物理，却不适合代替“等待两秒”的协程？

## 4. 模块复习

### 玩家、瞄准和战斗

- PlayerController为什么是调度者，不是所有功能的实现者？
- 鼠标屏幕坐标怎样变成Player的世界朝向？
- 位置、方向、normalized和sqrMagnitude分别表示什么？
- PlayerShooter、ProjectilePool、Projectile和Health各自的边界是什么？
- 预热20颗为什么不是最多只能同时存在20颗？
- SetActive、isLaunched和Rigidbody.MovePosition分别改变什么？

### 交互

- Input Action只决定什么时候交互，实际目标从哪里来？
- PlayerInteractor为什么查找IInteractable而不是WorldItemPickup？
- closestInteractable在哪里取得并保存实际组件引用？
- `closestInteractable.Interact(gameObject)`中双方分别是谁？
- WorldItemPickup和ExtractionZone为什么都要重新写Interact方法体？

### ItemData与背包

- 为什么名称和效果值放ItemData，quantity放InventorySlot？
- 多个世界物品和槽位怎样共享同一个ItemData资产？
- TryAdd为什么先堆叠、再找空格？
- InventorySlot为什么不知道自己是第几格？谁用下标选中它？
- 为什么效果成功后才TryConsume？
- 满包时世界物品为什么不能直接消失？
- 为什么新增能量饮料和绷带不需要新增拾取脚本？
- 世界物体的ItemData引用与GameSaveSystem的Known Items分别解决什么问题？
- 新物品没有加入Known Items时，为什么能拾取却不能正确读取存档？

### 背包UI

- `inventory.Slots[i]`和`slotViews[i]`各是什么？
- InventoryUI为什么循环六次，而InventorySlotView只处理一格？
- UI为什么只读背包，不直接修改槽位？
- Point和Left Click为None时，为什么Button绑定正确仍不能点击？
- PlayerStatusUI为什么每帧读取状态，却不需要每帧重写TMP文字？
- `$"饮水：{value}/100"`和`SetText("饮水：{0}/{1}", value, 100)`最终显示相同，建立显示内容的过程有什么不同？
- “可复用文字缓冲区”可以用什么生活例子解释？它复用的是Health数据、字符串变量还是TMP内部字符空间？
- `{0}/{1}`本身是不是缓冲区？`displayedHydration`负责实际绘制还是只负责记住上次显示值？
- 全透明Image为什么仍可能挡住按钮？

### 撤离与提示

- PlayerInventory、ExtractionZone、ExtractionResultUI分别回答什么问题？
- 无样本时为什么不触发InventoryChanged？
- MessageText未激活时，谁仍处于活跃状态并能接收Show？
- 重复提示为什么要停止旧协程？
- 为什么临时提示使用WaitForSecondsRealtime？
- 重载场景前为什么必须恢复timeScale？

## 5. 易错表达修正

| 容易说错 | 准确说法 |
|---|---|
| “按E后接口把任务分配下去” | 输入触发PlayerInteractor；它找到最近接口实现并直接调用实际对象 |
| “WorldItemPickup修改背包” | 它请求PlayerInventory.TryAdd；背包和槽位实际修改数据 |
| “HasItem检查到变化” | HasItem只查询是否持有，不修改也不通知 |
| “Invoke自动监测数据” | 代码修改成功后显式Invoke，事件才通知订阅者 |
| “SetActive让子弹自动飞” | 激活使生命周期执行，FixedUpdate中的移动代码才产生位移 |
| “对象池超过20次射击就扩容” | 同时活跃对象耗尽可用Queue时才扩容 |
| “拖入Canvas就是引用Canvas” | 字段类型为组件时，Unity保存Canvas上匹配的组件实例 |
| “GameMessageUI发事件” | 当前ExtractionZone直接调用`messageUI.Show()` |

## 简历驱动面试训练 2026-09-09

- 来源：`E:/Find jobs/李奇峰-Unity客户端与Gameplay开发-简历-更新版.docx`，只读，未修改简历。简历项目名为《撤离边界》，本地工程仍为Running。
- 范围：专业技能前四条（C#与AI辅助、Unity与Gameplay、功能设计、性能与工程）以及《撤离边界》。交通仿真、MAPPO、ML-Agents不是本聊天训练主体；不据此推断用户掌握程度。
- 目标：简历每个相关声明都能说清实际职责、数据/引用流向、调用链、设计理由、异常与测试证据；不承诺所有问题都能答对，不用背术语掩盖未实现或未验证内容。
- 用户当前特点：知道单点概念，但连接时常省略主语/目标/返回结果。先练“谁拿什么引用、调用谁、结果给谁”，再命名接口、多态、事件等术语。
- 每轮方法：只练一个简历声明、一个主问题；先不透露定位结论。用户用30—60秒说明，反馈分为正确、模糊、错误、待验证；只纠正关键断点，接一个追问或换场景，再让用户重述。避免连续长题库、同主题无限变式和过度防御性代码。

| 顺序 | 简历声明与实战锚点 | 需要能回答的追问 |
|---|---|---|
| 1 | 敌人行为、失效重选、动画事件命中 | 目标候选与有效性、FSM状态与攻击冷却、接口引用来源、TryAttack与命中分离、目标中途死亡或离开范围 |
| 2 | Health、事件、UI与协程 | 数据拥有者、发布/订阅/回调区别、启用后首次同步、重复通知定位、停止流程与恢复状态 |
| 3 | 统一交互、背包、配置与运行数据 | 接口调用者/实现者、TryAdd结果与消耗、共享配置与独立数量/冷却、扩展数值与扩展行为 |
| 4 | JSON持久化、集合 | 序列化与文件IO分开、ID查回引用、Dictionary键值/foreach/out、配置丢失规则、异常与合法性检查 |
| 5 | 子弹与飘字对象池 | Queue空闲对象、借出重置归还、扩容与泄漏区别、生命周期中断、容量上限只是扩展讨论 |
| 6 | Profiler与性能优化 | 定位到函数、整数缓存原本已有、TMP格式化减少分配、GC Alloc不等于回收、VSync等待不等于CPU计算、同条件前后比较 |
| 7 | Windows打包、测试、Git、AI辅助 | Editor/Development/Release区别、旧构建与当前代码边界、已做本地Git不等于复杂协作、解释调整生成代码并提供真实排错例子 |

### 事实边界与高风险表述

- 本次查读了简历正文、实际近战/动画桥接/UI/存档代码和`操作与作用.md`的采样/构建记录。文档中的既往测试是历史证据，本轮没有重新运行Unity或重打包。
- Profiler旧UI记录：原本已缓存显示整数，饮水整数变化时出现78 B；改用TMP数值SetText后同条件选中帧为0 B。不要说之前每帧都生成、整个游戏永远0 GC、或帧率因此显著提高。
- 持续战斗时对象数稳定是测试窗口内的观察，不是彻底证明无内存泄漏；保留截图/条件比背总内存数字重要。对象池与飘字的新功能不可自动继承所有旧性能测试结果。
- 近战在TryAttack缓存目标并记录冷却，动画事件调用ApplyPendingHit时取出并清空目标，再检查非空、存活、对象激活及当前水平距离。超出hitRange就挥空，不退还冷却、不改打其他目标。2026-09-10已补命中帧距离复查，待Play验证；这是原目标根位置的水平距离判定，不是精准武器碰撞、扇形或遮挡检测。
- Release完整流程已存在历史验证；之后新增功能不能直接宣称已在最新Release中验证。2026-09-10字典改动获用户F5/F9成功反馈；CSV导入编译与解析测试通过，用户也确认菜单导入、资产字段更新与Play效果全部成功，截图显示导入2条配置。
- 批量奖励临时试放/原子提交、精英强化、多人治疗站、近远程共用冷却、物品使用接口新设计均只是问答设计，不写成项目已有功能。未知物品占位未做，目前找不到ID跳过并警告。
- 第一轮待问：按时间顺序讲近战从找到玩家到扣血的过程；只用角色/组件、动作、结果，不强制函数名，再根据实际回答追问。

## 6. JSON阶段复习

### 2026-09-05：字典索引学习收尾

- 状态更新：2026-09-10用户确认物品ID字典F5/F9回归成功；CSV样品菜单导入与玩法验证也已完成。后续讲解重点是菜单注册与点击调用、CSV解析、按ID查资产和编辑器保存，不再重复要求相同验证。
- 用户已理解：foreach逐个取出数组中的资产引用，Add把ID与该引用登记进字典；字典找的是物品种类，数量来自存档。
- 需巩固：Awake提前建立字典，不是每次读档重建；ID本身不能自动找到资产，依赖提前登记的映射；查到的是已有资产引用，不是new出一个实例。循环变量item下一轮指向别处，不改变已存入字典的引用。
- 关键逻辑链：Inspector资产数组 → Awake建立ID/资产引用字典 → 读档取得各槽位ID和数量 → 按ID查已有引用 → 引用与数量恢复槽位。
- 自己能说：存档提供ID和数量，字典提供资产引用，背包把两者组合起来恢复格子。功能结果不变，查找方式由逐项比较变为按键查询。
- 下次只问三点：字典的键和值分别是什么？为什么ID必须提前登记且不能重复？找回资产引用与创建实例有什么区别？
- 讲解偏好：先用途与前后差异，再格式和代码关键点；用短例子与逻辑链，不用大量术语或重复长文。泛型语法归C#知识库，映射原理归数据结构知识库，资产与生命周期应用归Unity知识库。


应能独立复述：

1. F5如何把运行时状态转换为JSON文件。
2. `CreateSaveData`负责哪一段，为什么不直接把Health和Inventory组件写进JSON？
3. 为什么存`itemId`而不是直接存`ItemData`引用。
4. `new GameSaveData`创建了什么，为什么存档类只放字段。
5. F9如何通过Known Items找回物品并恢复六格槽位。
6. 为什么恢复状态要调用各数据拥有者的public入口。
7. 为什么背包恢复六格后只触发一次`InventoryChanged`。
8. 当前存档为什么只能称为“玩家状态存档”，不能称为完整世界存档。

### 容易说错

| 容易说错 | 准确说法 |
|---|---|
| `ReadAllText`把JSON转换成字符串 | JSON本来就是文本；该方法把磁盘文字读取到内存字符串 |
| `FindItem`检查ID和数量 | 它只把ID映射为已有`ItemData`资产引用，数量单独恢复 |
| 返回`false`会进入`catch` | `false`是正常返回；只有抛出异常才进入`catch` |
| 恢复函数先返回`true`再修改 | 先整体校验，再循环修改，完成后通知并返回`true` |
| 快照会跟着运行背包变化 | 快照保存复制值；重新调用`CreateSaveData`才会取得新状态 |

## 7. Profiler阶段复习

- 60 FPS为什么对应约16.67 ms，而不是“CPU显示60”？
- Timeline和Hierarchy分别回答什么问题？
- `Total`、`Self`、`Calls`、`GC Alloc`分别表示什么？
- 为什么某帧GC Alloc为0 B，不能证明整个游戏始终无分配？
- 为什么Editor中的1.30 GB不能写成游戏运行内存？
- `WaitForSignal`很长时，为什么不能直接判定该函数性能差？
- 如何用静止、持续射击和扩容三组数据验证对象池，而不是只讲理论？
- `GC Alloc`为0 B为什么只能说明没有新托管分配，不能说“当前没有内存占用”？
- CPU曲线出现尖峰后，为什么必须先区分PlayerLoop和EditorLoop？
- 持续射击时某帧Physics为0，为什么不能用该帧判断子弹物理移动成本？
- `Total`与`Time ms`、`Self`与`Self ms`分别是什么关系？
- `1.29/1.74 GB`为什么不是“已经用掉固定内存上限的1.29 GB”？
- Memory Simple为什么看起来固定，它显示选中帧还是全部帧的总和？
- PlayerLoop的Total为什么不包含旁边的EditorLoop？
- `Projectile.FixedUpdate`的Time与Self相等，说明当前层级中发生了什么？
- 为什么只看到最近300帧，旧帧还能不能通过横向滚动找回？
- 扩容后Game Objects增加12而Scene Objects增加84，为什么两个数字不同？
- 搜索不到`Instantiate`时，为什么不能否定对象池发生了扩容？
- 新字符串在哪一帧计入`GC Alloc`，旧字符串又在什么时候才有资格被GC回收？
- 为什么周期出现78 B分配，不代表GC也按相同周期执行？
- 状态UI原来的第一层缓存是否存在？这次`SetText`实际优化的是哪一层？
- 为什么PlayerLoop约16.67 ms但其中15.21 ms在等待时，不能说CPU计算已经跑满一帧预算？
- `Mono.JIT`为什么常出现在某条代码第一次执行的帧，后续相同路径为什么会变小？
- 为什么`Projectile.OnTriggerEnter`显示43 ms时不能立刻说“物理碰撞太慢”，还要继续展开子节点？
- `Profiler.Callstack`与`Debug.Log → StackTraceUtility.ExtractStackTrace`分别是谁产生的，为什么不能混为同一功能？
- 注释`Health.TakeDamage`成功日志后，2.8 KB降为48—72 B说明了什么？为什么不必继续追求每个碰撞帧绝对0 B？
- 为什么先用Development Player连接Profiler，再用非Development Release做完整冒烟测试？两者分别回答什么问题？
- Release中F5生成的存档为什么出现在`Running.exe`同级的`SaveData`目录，而不是项目Assets目录？

### 本轮先用自己的话回答

1. `$"饮水：{currentHydration}/100"`中的值从88变成87时，哪个新对象被创建，哪个旧对象可能失去引用？
2. `GC Alloc`和`GC`分别记录“制造”还是“清理”，它们是否必须发生在同一帧？
3. 用“先判断需不需要做，再决定必须做时怎么少制造垃圾”复述状态UI的两层优化。

### 本轮已掌握的复述

```text
Profiler发现78 B GC Alloc
→ Hierarchy定位到PlayerStatusUI.Update
→ 确认显示整数变化时，字符串插值会创建新的完整托管字符串
→ 旧字符串失去引用后等待以后由GC回收
→ 保留整数缓存判断，改用TMP SetText和内部字符缓冲区
→ 同条件复测GC Alloc降为0 B，周期分配尖峰消失
```

半专业表达：第一层记录上次显示的整数，数值没变就不刷新；第二层数值变化时用`SetText`复用TMP字符空间，避免重新创建完整字符串，减少托管内存分配和GC压力。

### 战斗Profiler复述链

```text
持续战斗发现命中帧分配和尖峰
→ 展开Projectile.OnTriggerEnter
→ 继续定位到LogStringToConsole
→ 确认Health.TakeDamage成功日志是主要来源
→ 只注释高频日志并重新构建
→ GC Alloc由2.8 KB降至48—72 B
→ 对象数量和总内存保持稳定，对象池复用通过
```

### Release交付复述链

```text
Development Player已经完成性能定位和前后对比
→ 生成不连接Profiler的Windows Release
→ 按玩家顺序测试移动、战斗、交互、背包、物品和撤离
→ 用F5/F9确认构建后的文件路径和数据恢复
→ 用重新开始确认暂停状态与场景重载
→ 全流程无报错，说明本地MVP可以独立运行和演示
```

## 8. 状态机与攻击策略复习

### 已验证逻辑链

```text
敌人计算目标距离
→ DecideState得到nextState
→ ChangeState只登记真正发生的变化
→ ExecuteState根据currentState执行行为
→ Attack通过IEnemyAttack统一请求
→ GetComponent找到同一敌人上的具体策略实例
→ 多态分派到MeleeEnemyAttack或RangedEnemyAttack
→ 具体策略检查冷却并扣血或请求对象池发射
```

### 自测

1. `currentState`和`nextState`分别记录什么？为什么相同状态不需要重复切换？
2. 冷却未结束时敌人是否仍处于Attack？`TryAttack`为什么返回`false`？
3. 接口、具体实现、接口变量中的实际实例分别是什么？
4. `GetComponent<IEnemyAttack>()`创建新攻击对象了吗？实际怎样找到`MeleeEnemyAttack`？
5. 为什么不能说“把攻击功能放进接口”？功能实际移动到了哪里？
6. 什么是多态分派？什么是策略模式？两者与接口是什么关系？
7. 近战伤害、远程子弹伤害、攻击冷却、检测距离分别属于谁？
8. `Time.time`和`nextAttackTime`怎样组成攻击冷却？
9. `?.`和`条件 ? x : y`分别是什么？
10. `FindGameObjectWithTag("Player")`遇到多个Player会有什么问题？Tag为什么不能完整表达敌我关系？

### 词汇复述

```text
通过接口抽象统一入口，利用多态分派不同攻击实现。
使用策略模式把行为选择与具体攻击实现解耦。
使用ScriptableObject配置驱动，让同一套代码读取不同物品定义。
```

## 9. Health事件职责（已验证）

先回答五个职责问题：生命是谁的数据、谁能修改、攻击脚本是请求者还是执行者、变化后谁通知、最后谁显示。

项目已实现并验证以下事件：

```text
Damaged(实际伤害)
→ 受伤闪烁、命中音效、伤害飘字

HealthChanged(当前生命, 最大生命)
→ 血条、生命文字、治疗和读档刷新

Died()
→ 停止AI、死亡表现、掉落和结算
```

回血只需要`HealthChanged`，受伤闪红使用`Damaged`，Projectile不直接认识UI，`Died`只在生命首次归零时通知死亡处理。

## 10. Health事件与协程复习

### 可以直接复述的逻辑链

```text
我把生命数据和表现拆开：攻击者只向Health请求伤害，Health完成校验、扣血和死亡判断后发布事件。
生命UI订阅HealthChanged，受伤闪红订阅Damaged，独立死亡组件订阅Died。
死亡样品先关闭玩法与碰撞，再用协程让视觉子对象逐帧倒下，短暂停留后让根对象失活。
这样数据、规则和表现职责清楚，以后增加音效、动画或伤害数字时不用继续修改攻击脚本。
```

### 关键自测

1. 为什么UI启用时还要主动读取一次生命，不能只等待未来的`HealthChanged`？
2. `Damaged`、`HealthChanged`、`Died`分别适合通知哪些表现？
3. 为什么连续受伤时要先停止旧闪红协程？
4. 调用返回`IEnumerator`的协程方法与调用`StartCoroutine`有什么区别？
5. `yield return null`为什么能让倒下过程被肉眼看见？如果删掉会发生什么？
6. 30 FPS下速度为5米/秒时，每帧约移动多少米？0.1秒经过0.4秒过程的百分之多少？
7. 为什么`deathCoroutine = null`既出现在正常结束处，也出现在`OnDisable`的中断清理处？
8. 为什么当前只旋转`VisualRoot`，而不旋转带碰撞和逻辑的根对象？
9. 如果以后让死亡对象复活或回对象池，除重新激活根对象外还需要重置哪些状态？

## 11. 本轮复习记录

### 已经能够说明的关键逻辑

- 状态机按`DecideState → ChangeState → ExecuteState`工作：先算应处状态，只在变化时切换，再按当前状态执行行为。
- `MeleeEnemyAttack`和`RangedEnemyAttack`实现同一个`IEnemyAttack`；`GetComponent<IEnemyAttack>()`查找已有组件实例，接口引用通过多态分派执行具体攻击。
- 对象池用`Queue`借出和归还运行时实例，减少高频`Instantiate/Destroy`；`ScriptableObject`保存共享数据资产，二者职责不同、不能替代。
- 玩家交互系统查找最近的`IInteractable`并发起统一调用；`WorldItem`负责请求拾取，只有`TryAddItem`成功后才让世界对象消失。
- `inventory.InventoryChanged += Refresh`表示UI订阅背包变化事件，并登记`Refresh`回调。口诀是“左边是通知，右边是收到通知后执行的方法”。
- 生命周期记法：`Awake`建立组件基础，`OnEnable/OnDisable`管理活跃期间的订阅边界，`Start`执行首次启动逻辑；`FixedUpdate`主要服务固定步长的物理模拟。
- 存档链是“创建纯数据DTO快照 → `ToJson`生成字符串 → 写入文件”；物品保存稳定`itemId`，读取时在Known Items中重建`ItemData`资产引用。
- 预期中的业务失败使用`bool`返回值，文件权限、损坏数据等意外异常使用`try/catch`。

### 内存泄漏重点表达

> 内存泄漏不是“出现了GC Alloc”，而是已经不需要的对象仍被引用，导致长时间重复操作后，内存或对象数量持续上涨、无法稳定。

记忆句：

```text
偶发尖峰看分配，持续爬升查引用；上涨后能稳定，通常只是正常扩容。
```

对象池容量增长后保持稳定不属于泄漏；`GC Alloc`是每帧新分配量，也不能用“这条线是否下降”直接判断泄漏。

### 下次复习起点

1. `GC Alloc`与真正的GC回收有什么区别？
2. 高频`Debug.Log`为什么同时带来托管分配和CPU开销？
3. `Mono.JIT`首次尖峰代表什么，为什么后续通常恢复？
4. Development Player与Release Player分别适合验证什么？

## 12. 对象池伤害飘字与面试场景训练

### 已验证逻辑链

```text
Health发布实际伤害
→ Emitter把事件转换为头顶显示请求
→ Pool借出一个完整文字对象
→ View重置并显示本次数字
→ 表现结束后回到Queue
```

可以复述：

> 攻击来源只向生命组件发起请求，生命组件完成实际变化后通过事件通知表现。伤害飘字由独立Emitter转发给对象池，View每次复用前重置运行状态，完成表现后回池。这样伤害规则不依赖UI，高频文字也避免反复创建销毁。

### 面试设计判断

- 不必强行背很多层，先说清“谁请求、谁裁定、谁通知、谁表现、怎样验证”的逻辑链。
- 功能相似不等于必须抽接口；只有调用者需要忽略具体类型并统一调用时，接口才有价值。
- 方法表示主动请求，事件表示完成后通知；不能把`TryHeal`方法说成可订阅事件。
- 治疗成功反馈、绿色数字、治疗站、暴击和命中点均未实现，回答时只能作为扩展设计。

### 排错场景：一次命中出现多个相同数字

```text
一次输入
→ TakeDamage应为1次
→ Damaged应发布1次
→ HandleDamaged应执行1次
→ Pool.Show应调用1次
→ View应激活1个
```

若`TakeDamage`为1次但回调为3次，检查重复订阅或重复Emitter；若借出为1次但复用后半透明，检查View是否恢复颜色、时间等状态。找到调用链中第一处异常，可以把问题限制在相邻职责之间。

### 下次项目场景题

玩家满血使用医疗包，生命没有变化但物品仍被消耗：从数字键输入、物品使用、Health返回结果到背包扣除，寻找第一个违反“治疗成功才消耗”的位置，并列出满血、部分缺血、治疗溢出、无物品和重复输入测试。

## 13. 治疗结果通知与Animator逻辑链

### 已经能够说明的逻辑

- `TryHeal`负责请求并返回是否成功，`Healed(actualHeal)`负责通知实际恢复量；方法是请求，事件是结果通知。
- Health拥有生命上限与实际恢复计算，物品使用者根据bool结果决定消耗，Emitter把事件转为表现请求，伤害与治疗共享同一浮动文字池。
- 代码AI状态机决定敌人做什么，Animator状态机只决定VisualRoot播放什么；两者通过`StateChanged`和`Attacked`事件连接。
- `State`整数表示持续状态，`Attack` Trigger表示一次成功攻击，攻击冷却仍由攻击策略负责，不能复制到Animator中。
- 参数哈希是固定的参数编号，`SetInteger`修改的是Animator内部该参数的值，不是修改readonly编号。
- 本地正Z突进依赖根对象先朝向目标；AI负责方向，动画负责本地视觉位移。

### 面试版逻辑链

> 我把AI决策、攻击规则和动画表现分开：AI进入Attack状态后会持续尝试攻击，攻击策略通过目标与冷却检查后只发出一次开始攻击通知；Animator播放到命中帧时，再通过Animation Event结算实际伤害。根对象负责移动、朝向和碰撞，VisualRoot只负责表现，因此伤害时机能与画面对应，也不会让动画污染玩法坐标。

## 14. 阵营过滤与最近目标选择（已验证）

### 逻辑链

```text
每隔0.25秒检测一次范围
→ OverlapSphereNonAlloc把Collider写入复用数组
→ 通过Collider向父级查找Health
→ 排除自己、死亡目标和非敌对阵营
→ 比较有效候选的距离
→ 返回最近目标的Health与Transform
→ AI根据距离切换状态并执行行为
```

这和E交互属于同一种问题结构：E交互筛选最近的`IInteractable`，敌人搜索筛选最近的存活敌对目标。两者都可以概括为“候选、筛选、比较、执行”。

### 可以随口说

> 我没有让敌人固定依赖Player Tag，而是把阵营身份拆成独立组件，再用NonAlloc球形检测定时取得候选。搜索器排除自己、死亡对象和非敌对阵营，然后保留最近目标。AI只消费搜索结果，Health继续只负责生命规则。这样同一套搜索可以支持多个玩家、诱饵或其他阵营目标。

### 下次继续

1. 为什么`Physics.OverlapSphereNonAlloc`是方法，而`Collider[]`只是缓冲区？
2. 为什么`StateChanged`不负责让Chase持续移动？
3. `IEnemyAttack`如何让同一句`TryAttack`多态执行近战或远程实现？
4. Dummy死亡后敌人为什么会重新选择玩家？
5. 如果动画播放但没有扣血，应从Animation Event链路的哪一段开始检查？
6. 如果多个目标距离相近，但需要优先攻击玩家而不是诱饵，现有“只按距离”规则应扩展什么数据？
