# 通过项目理解

> 目标：不仅让功能运行，还要能独立说明怎么设计、为什么这样设计、有什么取舍。

## 1. 每个模块的学习顺序

1. 需求：先写正常结果、失败情况和范围限制。
2. 朴素方案：说明最直接做法，以及它会产生的问题。
3. 职责拆分：每个类只回答一个业务问题。
4. 设计选择：解释为什么使用接口、引用、数组、集合或对象池。
5. 执行流程：从输入开始，跟到最终状态变化。
6. 代码对应：每段代码必须对应前面的需求或设计选择。
7. 边界验证：测试成功、失败、上限、空引用等情况。
8. 独立复述：能脱离代码讲清职责、原因、取舍和验证结果后才进入下一模块。

## 2. 讲解固定结构

### 整体

- 要解决什么问题。
- 数据从哪里来、流向哪里。
- 哪个对象负责选择、调用、处理和保存结果。

### 局部

- 类和接口的职责。
- 字段、参数、返回值分别代表什么。
- Unity 生命周期中的调用时机。
- 使用的 C# 基础及其实际作用。

### 设计

- 为什么采用当前方案。
- 不这样做时会出现什么问题。
- 当前方案的优点、代价和适用范围。
- 面试时如何用自己的话说明。

## 3. 理解完成标准

- 能根据需求自己画出调用流程。
- 能说清每个类为什么存在，而不只会念代码。
- 能区分类型、实例、引用、接口、参数和返回值。
- 能说明至少一个替代方案及当前选择的理由。
- 能预测正常情况和边界情况的运行结果。
- 能修改一个参数或规则，并判断需要改哪些代码。

## 4. 当前学习检查点：物品与背包

暂不继续增加功能，先能独立回答：

- E 键、PlayerInteractor、IInteractable 各负责什么。
- `InteractionName`、目标对象和 `interactor` 的区别。
- ItemData、WorldItemPickup、PlayerInventory、InventorySlot 各负责什么。
- 为什么同一种物品共享 ItemData 引用。
- 为什么六格背包使用数组，槽位使用 class 实例。
- 为什么 TryAdd 先堆叠、再找空格，并返回 bool。
- 为什么确认添加成功后，世界物品才能消失。
