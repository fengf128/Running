# 操作与作用

> 只记录实际操作、原因和运行结果；原理见 `知识.md`，设计链见 `思路.md`。

## 1. 项目初始化

### 创建与整理

- 操作：Hub → Unity 2022.3.57f1c1 → Universal 3D。
- 原因：Sample 模板内容过多，空白模板更适合作品。
- 操作：在 `Assets/_Project` 下按资源类型建立目录。
- 操作：将 `SampleScene` 另存为 `Assets/_Project/Scenes/GameScene.unity`。
- 结果：得到独立、干净的 URP 项目和正式主场景。

### 处理编辑器警告

- 现象：默认布局无法完整加载。
- 操作：清空 Console，切换 Tall/Wide 后复查。
- 结果：确认是 AppData 中的全局布局问题，不影响项目编译。

### 清理 Plastic/UOS

- 原因：Plastic/UOS（Unity Online Services，Unity 在线服务）工作区重复绑定，且已决定使用 Git。
- 操作：关闭 Unity/Hub，备份后移除相关包和宏。
- 结果：无关云服务和重复报错已清除。
- 备份：`E:\Unity3D\Mission\Running-setup-backup\2026-08-19-plastic-uos`。

### 配置 Git

- 操作：`git init -b main` → `git add .` → `git commit`。
- 作用：保存可回退的开发检查点。
- 结果：建立 `80cf9f2 Unity项目基线`。

## 2. Input System

### 安装

- 操作：Package Manager → Input System → Install。
- 操作：Active Input Handling → Input System Package (New)。
- 原因：把玩家意图与具体按键解耦。
- 结果：Input System 1.11.2。

### 创建与配置输入资源

- 操作：Create → Input Actions → `PlayerInputActions`。
- 结果：建立 `Gameplay` Action Map。
- Move：`Value + Vector2`，2D Vector → WASD。
- Aim：`Value + Vector2`，`Mouse/Position`。
- Fire：`Button`，`Mouse/Left Button`。
- Interact：`Button`，`Keyboard/E`。

### 使用槽位输入

- 操作：在 Gameplay 新增 UseSlot1～UseSlot6，均为 Button，分别绑定数字键 1～6。
- 结果：生成类已包含六个强类型 Action。
- 作用：按槽位编号请求使用物品，不直接读取 Keyboard。
- Project-wide Input Actions 未指定不影响当前方案；项目自行创建并启用 PlayerInputActions 实例。

### 生成代码与排错

- 操作：Generate C# Class → Apply。
- 结果：`PlayerInputActions.cs` 已生成并编译。
- 现象：`type is not a supported string value`。
- 检查：调用栈来自 `InputControlPathEditor`，且没有 `error CS`。
- 结论：属于编辑器重绘异常，不影响输入代码。

## 3. 玩家控制

### 场景对象

- Ground：Plane，Scale `(4, 1, 4)`，提供地面和碰撞。
- Player：Capsule，Position `(0, 1, 0)`，使用 CharacterController。
- DirectionMarker：显示 Player 正前方。

### 移动与重力

- 文件：`Assets/_Project/Scripts/Player/PlayerController.cs`。
- 操作：读取 Move，计算水平速度、重力和最终位移。
- 结果：WASD、接地和碰撞运行成功。

### 相机跟随

- 文件：`Assets/_Project/Scripts/Camera/TopDownCamera.cs`。
- 操作：Target 绑定 Player，在 LateUpdate 中跟随并看向目标。
- 结果：俯视角跟随运行成功。

### 鼠标瞄准

- 修改：`Assets/_Project/Scripts/Player/PlayerController.cs`。
- 流程：屏幕坐标 → 相机射线 → 地面交点 → Player 朝向。
- 结果：鼠标瞄准运行成功，无空引用和零方向错误。

## 4. 战斗系统

### 创建组件

- `Health.cs`：管理生命、受伤和死亡停用。
- `PlayerShooter.cs`：从 FirePoint 发射并限制射速。
- `Projectile.cs`：处理移动、碰撞、伤害、寿命和回收。
- `ProjectilePool.cs`：预创建并复用子弹。

### 配置

- Projectile Prefab：SphereCollider、Kinematic Rigidbody、Is Trigger。
- ProjectilePool：绑定 Prefab，Initial Size 为 20。
- PlayerShooter：绑定 FirePoint、Pool，冷却为 0.15 秒。
- Projectile：速度 18、寿命 2 秒、伤害 10。

### 验证

- TargetDummy 初始生命 30。
- 三次命中：20/30 → 10/30 → 0/30。
- 结果：射击、碰撞、伤害、死亡和回收均运行成功。

## 5. 敌人

### ChaserEnemy

- 文件：`Assets/_Project/Scripts/Enemies/ChaserEnemy.cs`。
- 操作：按距离切换待机、追击和近战攻击。
- 结果：Player 生命降至 90/80/70/60；敌人生命降至 20/10/0。

### RangedEnemy

- 文件：`Assets/_Project/Scripts/Enemies/RangedEnemy.cs`。
- 操作：进入 12 米后转向 Player，并按 1.5 秒冷却发射。
- 操作：让玩家和敌人共享 Pool，每发子弹记录发射者 Health。
- 结果：双向伤害成功；远程敌人在 0/30 时正确停用。

## 6. 统一交互

### 统一规则

- 文件：`Assets/_Project/Scripts/Interaction/IInteractable.cs`。
- 作用：让物品、任务物和撤离点使用相同交互入口。

### 玩家检测

- 文件：`Assets/_Project/Scripts/Interaction/PlayerInteractor.cs`。
- 操作：E 键触发 2 米球形检测，过滤接口并选择最近目标。
- 配置：范围 2，LayerMask 暂为 Everything，结果数组长度 16。

### 测试

- 文件：`Assets/_Project/Scripts/Interaction/TestInteractable.cs`。
- 场景：PlayerInteractor、TestBoxA、TestBoxB 已保存。
- 结果：范围外不触发；范围内只交互最近对象。

## 7. 物品与背包

### 物品数据

- 文件：`Assets/_Project/Scripts/Items/ItemData.cs`。
- 作用：用 ScriptableObject 保存 ID、名称、类型、堆叠上限和效果数值。

### 六格背包

- 文件：`Assets/_Project/Scripts/Inventory/InventorySlot.cs`、`PlayerInventory.cs`。
- 作用：固定创建六个槽位；添加时先堆叠，再使用空槽。

### 消耗槽位与饮水

- InventorySlot 新增 TryConsumeOne：空槽失败；成功后数量减一，归零时清除 ItemData 引用。
- PlayerInventory 新增 TryConsume(slotIndex)：检查下标、选择槽位、调用消耗并触发 InventoryChanged。
- PlayerHydration：上限 100、初始 50、每秒减少 1；TryRestore 成功时恢复且不超过上限。
- PlayerItemUser：按 ItemType 把 Water 交给 PlayerHydration，把 Medical 交给 Health；效果成功才消耗。
- PlayerController：数字键 1～6 映射数组下标 0～5。
- RequireComponent 在运行时检查到 Player 缺少 PlayerItemUser 后自动补齐；退出 Play 后仍需确认组件并保存场景。
- 结果：两瓶水均成功恢复 25 点饮水值并从槽 1 消耗；空槽不报错。
- 独立 C# 编译检查：0 警告、0 错误。

### 医疗物品

- 创建 Item_Medicine：ID `medicine_small`、类型 Medical、堆叠上限 2、效果值 30。
- 复制现有拾取物并改为 MedicinePickupA/B，共用统一交互、背包和 UI 流程。
- 结果：满血使用不消耗；受伤后恢复 30 点生命、数量减一并刷新 UI。
- 饮水和治疗均已运行验证。

### 任务物品与撤离条件

- 创建 Item_QuestSample：ID `quest_sample`、类型 Quest、堆叠上限 1、效果值 0。
- 场景加入两个样本拾取物；按数字键不能直接使用和消耗。
- PlayerInventory 新增 HasItem，只查询指定 ItemData，不修改槽位。
- ExtractionZone 实现 IInteractable，检查 Player 背包是否持有 Required Item。
- 撤离点 BoxCollider 使用 Is Trigger，并引用 Item_QuestSample。
- 结果：无样本时失败；拾取样本后成功；成功后重复按 E 不重复结算。

### 撤离结算 UI

- Canvas 下创建全屏半透明 `ExtractionResultPanel`，包含标题和详情 TMP 文字。
- `ExtractionResultUI` 开始时隐藏面板；成功后读取 Player 的生命和饮水公开属性并填写文字。
- `ExtractionZone` 直接引用一个 `ExtractionResultUI`；条件满足后调用 `ShowSuccess`。
- `Pause Game` 开启后把 `Time.timeScale` 设为 0，停止本局模拟。
- 结果：显示“撤离成功”、实验样本、生命 `100/100`、饮水 `68/100`，并暂停游戏。
- 调整项：加宽 `DetailsText` 可避免“实验样本”自动换行。

### 重新开始与按钮排错

- 在结算面板创建 TMP Button，`On Click()`绑定 `ExtractionResultUI.RestartGame()`。
- 将 GameScene 加入 Scenes In Build；方法恢复 `Time.timeScale`后按 buildIndex 重新加载当前场景。
- 现象：按钮显示但点击无反应，Console无错误。
- 原因：Input System UI Input Module误用仅含 Gameplay的 PlayerInputActions，Point和Left Click均为 None。
- 操作：重置 Input System UI Input Module，改用带完整 UI Action Map的 DefaultInputActions。
- 结果：按钮获得鼠标位置和左键输入；点击后场景、Player、背包、生命和饮水恢复初始状态。

### 撤离失败提示

- Canvas下创建独立 MessageText；不放入开始时隐藏的结算面板。
- GameMessageUI收到字符串后填写文字、激活对象并启动两秒隐藏协程。
- ExtractionZone在 HasItem返回false时直接调用 `messageUI.Show`，不修改背包。
- 重复按 E 时先停止旧协程，再启动新协程，避免旧计时提前隐藏新提示。
- 结果：无样本时显示“缺少任务物品：实验样本”，两秒后自动消失；有样本时仍正常结算和重开。

### 世界拾取

- 文件：`Assets/_Project/Scripts/Items/WorldItemPickup.cs`。
- 作用：实现 IInteractable；添加成功才停用世界物体。
- 数据：已创建 `Assets/_Project/Data/Items/Item_Water.asset`，上限 3，效果值 25。
- 场景：Player 已添加 PlayerInventory；WaterPickupA、B 已引用 Item_Water。
- 结果：首次拾取进入槽 1，第二次拾取堆叠为 2/3，六格数组运行成功。

## 8. 背包 UI

### UI 与布局

- 操作：创建 Canvas、InventoryPanel 和六个 SlotView。
- Canvas Scaler：`1920×1080`，Match 为 `0.5`；作用是适配不同分辨率。
- InventoryPanel：左上锚点，大小 `340×300`，位置 `(20,-20)`。
- Vertical Layout Group：内边距 10、间距 6、控制宽高、横向填满；作用是自动纵向排列六格。
- Layout Element：每格首选高度 36；TextMeshPro 字号 24。

### 输入模块排错

- 现象：Standalone Input Module 调用旧 `UnityEngine.Input`，与 New Input System 冲突。
- 操作：改用 Input System UI Input Module。
- 结果：旧输入异常消失；当前 UI 为只读显示，点击操作尚未实现。

### 字体

- 操作：导入 TextMeshPro Essentials、Noto Sans CJK SC 原字体和 SIL OFL 许可证。
- 操作：创建 Dynamic SDF 字体，图集 `2048×2048`，开启 Multi Atlas Textures。
- 作用：按需加入中英文字形，避免预生成全部汉字图集。
- 证据：`Assets/_Project/FONTS/NotoSansCJKsc-Regular.otf`、`LICENSE-Noto.txt`。

### 事件刷新

- `PlayerInventory`：添加成功后触发 `InventoryChanged`。
- `InventoryUI`：订阅事件，读取六个数据槽位并协调刷新。
- `InventorySlotView`：把一个槽位格式化为 TMP 文字。
- 结果：初始显示六个 Empty；两次拾取显示 `矿泉水 1/3 → 2/3`。
