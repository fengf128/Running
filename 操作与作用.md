# 操作与作用

> 只记实际操作；一项一条。

## 创建项目

- 原因：Sample 内容过多。
- 步骤：Hub → 2022.3.57f1c1 → Universal 3D。
- 作用：得到干净 URP 项目。

## 处理布局警告

- 步骤：Console Clear → 切换 Tall/Wide。
- 作用：确认警告来自全局布局，不影响项目。

## 清理 Plastic/UOS

- 原因：工作区重复绑定，项目改用 Git。
- 步骤：关闭 Unity/Hub → 备份 → 移除相关包和宏。
- 作用：消除无关云服务和重复报错。
- 备份：`E:\Unity3D\Mission\Running-setup-backup\2026-08-19-plastic-uos`。

## 配置 Git

- 步骤：`git init -b main` → `git add .` → `git commit`。
- 作用：建立可回退历史。
- 结果：`80cf9f2 Unity项目基线`。
- 检查点：`7c7b7c8 feat: 完成玩家移动相机与瞄准`。

## Input System

### 安装

- 原因：输入动作与按键解耦。
- 步骤：
  1. Package Manager → Input System → Install。
  2. Active Input Handling → Input System Package (New)。
- 结果：Input System 1.11.2。

### 创建输入资源

- 步骤：Create → Input Actions → `PlayerInputActions`。
- 作用：集中保存输入动作和按键。
- 结果：已建立 `Gameplay` Action Map。

### 配置 Gameplay

#### Move

- 类型：`Value + Vector2`。
- 绑定：2D Vector → WASD。
- 作用：输出二维移动方向。

#### Aim

- 类型：`Value + Vector2`。
- 绑定：`Mouse/Position`。
- 作用：输出鼠标屏幕坐标。

#### Fire

- 类型：`Button`。
- 绑定：`Mouse/Left Button`。
- 作用：触发开火。

#### Interact

- 类型：`Button`。
- 绑定：`Keyboard/E`。
- 作用：触发统一交互。

### 生成 C# 类

- 步骤：Generate C# Class → Apply。
- 作用：用强类型代码访问 Action。
- 结果：`PlayerInputActions.cs` 已生成并编译。

### 排查编辑器报错

- 现象：`type is not a supported string value`。
- 检查：调用栈来自 `InputControlPathEditor`，无 `error CS`。
- 结论：编辑器重绘异常，不影响本次生成结果。

## 建立项目目录

- 步骤：在 `Assets/_Project` 下按资源类型建目录。
- 作用：项目资源集中管理。

## 创建主场景

- 步骤：将 `SampleScene` 另存为 `GameScene`。
- 作用：建立正式开发场景。
- 结果：`Assets/_Project/Scenes/GameScene.unity`。

## 玩家控制

### 场景对象

#### Ground

- 对象：Plane，Scale 为 `(4, 1, 4)`。
- 作用：提供移动地面和碰撞。

#### Player

- 对象：Capsule，Position 为 `(0, 1, 0)`。
- 组件：CharacterController。
- 作用：作为玩家碰撞和移动主体。

### 创建移动脚本

- 路径：`Assets/_Project/Scripts/Player/PlayerController.cs`。
- 作用：读取 Move，处理水平移动和重力。
- 结果：已挂载到 Player，WASD 运行成功。
- 验证：无 C# 编译错误。

### 创建相机脚本

- 路径：`Assets/_Project/Scripts/Camera/TopDownCamera.cs`。
- 作用：在 LateUpdate 中保持俯视角跟随。
- 结果：已挂载，Target 指向 Player，运行成功。

### 实现鼠标瞄准

- 修改：`PlayerController.cs`。
- 方式：屏幕坐标 → 相机射线 → 地面交点 → 玩家朝向。
- 辅助：DirectionMarker 显示 Capsule 朝向。
- 结果：Camera 已绑定，鼠标瞄准运行成功。
- 验证：无编译、空引用和零方向错误。
