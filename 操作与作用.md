# 操作与作用

> 只记实际操作；一项一条。

## 创建项目

- 原因：Sample 内容过多。
- 步骤：Hub → 2022.3.57f1c1 → Universal 3D。
- 作用：得到干净 URP 项目。

## 处理布局警告

- 步骤：Console Clear → 切换 Tall/Wide。
- 作用：确认警告来自全局布局，不影响项目。

## 清理 Plastic/UOS

- 原因：工作区重复绑定，项目改用 Git。
- 步骤：关闭 Unity/Hub → 备份 → 移除相关包和宏。
- 作用：消除无关云服务和重复报错。
- 备份：`E:\Unity3D\Mission\Running-setup-backup\2026-08-19-plastic-uos`。

## 配置 Git

- 步骤：`git init -b main` → `git add .` → `git commit`。
- 作用：建立可回退历史。
- 结果：`80cf9f2 Unity项目基线`。
- 检查点：`7c7b7c8 feat: 完成玩家移动相机与瞄准`。
- 检查点：`29d1552 feat: 完成射击对象池与伤害验证`。
- 检查点：`2b8fa69 feat: 完成近战追击敌人验证`。

## Input System

### 安装

- 原因：输入动作与按键解耦。
- 步骤：
  1. Package Manager → Input System → Install。
  2. Active Input Handling → Input System Package (New)。
- 结果：Input System 1.11.2。

### 创建输入资源

- 步骤：Create → Input Actions → `PlayerInputActions`。
- 作用：集中保存输入动作和按键。
- 结果：已建立 `Gameplay` Action Map。

### 配置 Gameplay

#### Move

- 类型：`Value + Vector2`。
- 绑定：2D Vector → WASD。
- 作用：输出二维移动方向。

#### Aim

- 类型：`Value + Vector2`。
- 绑定：`Mouse/Position`。
- 作用：输出鼠标屏幕坐标。

#### Fire

- 类型：`Button`。
- 绑定：`Mouse/Left Button`。
- 作用：触发开火。

#### Interact

- 类型：`Button`。
- 绑定：`Keyboard/E`。
- 作用：触发统一交互。

### 生成 C# 类

- 步骤：Generate C# Class → Apply。
- 作用：用强类型代码访问 Action。
- 结果：`PlayerInputActions.cs` 已生成并编译。

### 排查编辑器报错

- 现象：`type is not a supported string value`。
- 检查：调用栈来自 `InputControlPathEditor`，无 `error CS`。
- 结论：编辑器重绘异常，不影响本次生成结果。

## 建立项目目录

- 步骤：在 `Assets/_Project` 下按资源类型建目录。
- 作用：项目资源集中管理。

## 创建主场景

- 步骤：将 `SampleScene` 另存为 `GameScene`。
- 作用：建立正式开发场景。
- 结果：`Assets/_Project/Scenes/GameScene.unity`。

## 玩家控制

### 场景对象

#### Ground

- 对象：Plane，Scale 为 `(4, 1, 4)`。
- 作用：提供移动地面和碰撞。

#### Player

- 对象：Capsule，Position 为 `(0, 1, 0)`。
- 组件：CharacterController。
- 作用：作为玩家碰撞和移动主体。

### 创建移动脚本

- 路径：`Assets/_Project/Scripts/Player/PlayerController.cs`。
- 作用：读取 Move，处理水平移动和重力。
- 结果：已挂载到 Player，WASD 运行成功。
- 验证：无 C# 编译错误。

### 创建相机脚本

- 路径：`Assets/_Project/Scripts/Camera/TopDownCamera.cs`。
- 作用：在 LateUpdate 中保持俯视角跟随。
- 结果：已挂载，Target 指向 Player，运行成功。

### 实现鼠标瞄准

- 修改：`PlayerController.cs`。
- 方式：屏幕坐标 → 相机射线 → 地面交点 → 玩家朝向。
- 辅助：DirectionMarker 显示 Capsule 朝向。
- 结果：Camera 已绑定，鼠标瞄准运行成功。
- 验证：无编译、空引用和零方向错误。

## 射击系统

### 创建子弹脚本

- 路径：`Assets/_Project/Scripts/Combat/Projectile.cs`。
- 作用：处理移动、存活时间和回收。
- 结果：Projectile Prefab 已完成物理配置。

### 创建对象池

- 路径：`Assets/_Project/Scripts/Combat/ProjectilePool.cs`。
- 作用：预创建并复用子弹。
- 结果：Pool 已绑定 Prefab，预热数量为 20。
- 验证：配置正确且无相关错误。

### 创建射击脚本

- 路径：`Assets/_Project/Scripts/Combat/PlayerShooter.cs`。
- 作用：按 FirePoint 发射子弹并限制射速。
- 结果：FirePoint、Pool 和冷却参数已完成绑定。
- 验证：引用正确且无相关错误。

### 创建生命组件

- 路径：`Assets/_Project/Scripts/Combat/Health.cs`。
- 作用：管理当前生命、受伤和死亡停用。
- 结果：TargetDummy 使用 30 点生命。

### 接入子弹伤害

- 修改：`Projectile.cs`。
- 作用：命中 Health 时造成 10 点伤害。
- 结果：三次命中依次得到 20/30、10/30、0/30。
- 验证：Projectile → Health 调用正确且无相关错误。

## 敌人

### 创建近战追击代码

- 路径：`Assets/_Project/Scripts/Enemies/ChaserEnemy.cs`。
- 作用：按距离待机、追击和攻击 Player。
- 结果：追击、每秒 10 点攻击和三发死亡均运行成功。
- 验证：Player 生命降至 90/80/70/60；敌人生命为 20/10/0。

### 创建远程敌人代码

- 路径：`Assets/_Project/Scripts/Enemies/RangedEnemy.cs`。
- 作用：在攻击范围内朝 Player 发射共享池子弹。
- 结果：远程敌人可攻击 Player；受到三次 10 点伤害后在 0/30 正确停用。

### 共享子弹池

- 修改：Projectile、ProjectilePool、PlayerShooter。
- 作用：每发子弹记录发射者，并忽略发射者自身碰撞。
- 结果：共享池双向伤害成功；远程敌人子弹使 Player 降至 80/70/60。
