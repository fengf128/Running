# 操作与作用

> 只记录项目中真实执行过的操作、关键配置、排错和运行结果。

## 1. 项目初始化

### 创建项目

- Hub选择Unity 2022.3.57f1c1和Universal 3D空白模板。
- 项目建立在`E:\Unity3D\Mission\Running`，正式场景保存为`Assets/_Project/Scenes/GameScene.unity`。
- `Assets/_Project`按Data、Input、Scenes、Scripts、UI、FONTS等类型整理资源。
- 移除未使用的Plastic/UOS集成，改用Git；备份保留在`Running-setup-backup/2026-08-19-plastic-uos`。

### Git

- 执行`git init -b main`、`git add`和`git commit`建立项目基线。
- 每个运行验证完成的模块建立独立提交，便于回退和提供证据。

## 2. 输入系统

### Gameplay输入

- Package Manager安装Input System 1.11.2。
- Active Input Handling设为Input System Package (New)。
- 创建`PlayerInputActions`和`Gameplay` Action Map。
- 配置：Move=WASD Vector2，Aim=Mouse Position，Fire=左键，Interact=E，UseSlot1—6=数字键1—6。
- 开启Generate C# Class并生成`PlayerInputActions.cs`。

### UI输入

- EventSystem使用Input System UI Input Module，不使用旧Standalone Input Module。
- Gameplay继续使用`PlayerInputActions`；UI模块使用带完整UI Map的`DefaultInputActions`。
- 已修复按钮无响应：原因为Point和Left Click均为None；重置UI输入模块后恢复点击。

## 3. 玩家与相机

### 场景配置

- Ground使用Plane提供地面与碰撞。
- Player使用Capsule外形和CharacterController，不重复保留CapsuleCollider。
- DirectionMarker标记Player正前方。
- Player出生点调整为`(0, 1, -12.29)`，避免开局立即进入敌人检测范围，保留观察和准备时间。

### 移动、相机和瞄准

- `PlayerController`读取Move，组合水平速度、竖直速度和重力后调用CharacterController.Move。
- `TopDownCamera`绑定Player，在LateUpdate保持固定偏移并看向目标。
- 鼠标屏幕坐标经相机射线与数学地面相交，交点减Player位置得到水平朝向。
- 验证：WASD、碰撞、重力、相机跟随和鼠标朝向正常。

## 4. 战斗与敌人

### 玩家射击

- 创建`Health`、`PlayerShooter`、`Projectile`和`ProjectilePool`。
- Projectile Prefab配置SphereCollider、Kinematic Rigidbody和Is Trigger。
- 参数：子弹速度18、寿命2秒、伤害10；射击冷却0.15秒；对象池预热20颗。
- TargetDummy从30/30依次降到20/30、10/30、0/30，命中、死亡和回收验证成功。

### 对象池

- 开始时预创建并停用20颗子弹。
- 玩家与远程敌人从同一Queue取出和归还子弹。
- 库存耗尽时创建新子弹；回收后加入Queue，形成永久扩容。
- 每发子弹记录发射者Health，避免命中自己。

### 两类敌人

- `ChaserEnemy`在12米内追击，进入攻击距离后按冷却近战扣血。
- `RangedEnemy`定时选择12米内最近的存活敌对目标，转向后每1.5秒请求对象池发射。
- 验证：玩家与两类敌人均能正确受伤，生命归零后停用。

### 状态机与攻击策略扩展

- 将`ChaserEnemy`原有距离分支显式整理为`Idle`、`Chase`、`Attack`枚举状态。
- `DecideState`只计算应有状态，`ChangeState`只在状态变化时登记，`ExecuteState`调用当前状态行为。
- 新增`IEnemyAttack.TryAttack(Transform)`统一攻击入口，返回值表示本次是否真正发动攻击。
- 近战伤害和1秒冷却移动到`MeleeEnemyAttack`；远程对象池、FirePoint和1.5秒冷却移动到`RangedEnemyAttack`。
- 近战敌人Inspector添加`MeleeEnemyAttack`；远程敌人添加`RangedEnemyAttack`并重新连接场景`ProjectilePool`和子物体`FirePoint`。
- Unity Play验证：三种近战状态能随距离往返切换；近战继续按1秒扣血；远程继续按1.5秒发射并正常命中；Console无红色错误。
- C#项目编译验证为0警告、0错误；代码提交为`7fceadc`和`a09a073`。

## 5. 统一交互

- 创建`IInteractable`，统一`InteractionName`和`Interact(GameObject)`调用形式。
- `PlayerInteractor`在E键请求时执行2米`OverlapSphereNonAlloc`。
- 结果数组固定为`Collider[16]`，当前LayerMask为Everything。
- 检测后过滤实现接口的组件，比较距离平方，只调用最近目标。
- 测试对象验证：范围外不触发，范围内只交互最近对象。

## 6. 物品、背包与UI

### 物品数据与拾取

- `ItemData`使用ScriptableObject保存ID、显示名、类型、堆叠上限和效果值。
- 已创建矿泉水、急救包、实验样本、能量饮料、绷带和强效急救剂六种资产。
- 场景以`Items`为父对象整理12个拾取物；每种物品的两个世界实例共享对应资产。
- `WorldItemPickup`实现IInteractable；`TryAdd`成功后才停用世界物体。

### 六格背包

- `PlayerInventory`创建六个`InventorySlot`实例。
- 添加顺序为先尝试已有堆叠，再寻找空格。
- 矿泉水首次进入槽1，再次拾取显示1/3→2/3；满包时拾取物不消失。

### 物品使用

- 数字键1—6映射数组下标0—5。
- `PlayerItemUser`按ItemType调用`PlayerHydration.TryRestore`或`Health.TryHeal`。
- 效果成功后才调用`TryConsume`；数量归零时清除ItemData引用。
- 验证：水恢复25点饮水、能量饮料恢复40点；急救包恢复30点生命、绷带恢复15点、强效急救剂恢复50点。
- 新物品的堆叠上限分别为能量饮料2、绷带3、强效急救剂1；拾取、堆叠、使用和满值不消耗均验证成功。

### 背包UI

- Canvas使用1920×1080参考分辨率和Match 0.5。
- InventoryPanel通过Vertical Layout Group排列六个SlotView，每格由Layout Element控制高度。
- `InventoryUI`订阅`InventoryChanged`，按下标把六格数据交给六个`InventorySlotView`。
- TextMeshPro使用Noto Sans CJK SC Dynamic SDF和Multi Atlas；源字体及SIL OFL许可证已保留。

### 玩家状态UI

- Canvas下创建透明`StatusPanel`、`HealthText`和`HydrationText`，挂载`PlayerStatusUI`。
- Inspector分别引用Player的`Health`、`PlayerHydration`和两个TMP文字组件。
- `Update`每帧读取状态，但只在显示整数变化时重写文字；整数变化时使用TMP `SetText`格式模板，避免字符串插值产生周期性临时字符串。
- 全屏透明Image关闭`Raycast Target`，避免看不见的UI遮挡结算按钮。
- 验证：饮水持续下降，受伤、治疗和F9读取后数值正确刷新，撤离后的重新开始按钮可点击。

## 7. 任务物与撤离

### 撤离条件

- 实验样本类型为Quest、堆叠上限1、效果值0。
- `PlayerInventory.HasItem`只查询，不修改数量。
- `ExtractionZone`实现IInteractable，Required Item绑定实验样本；BoxCollider使用Is Trigger。
- 验证：无样本失败，有样本成功，成功后重复按E不重复结算。

### 结算与重新开始

- Canvas下创建全屏半透明ExtractionResultPanel、标题、详情和RestartButton。
- `ExtractionResultUI`显示任务物、生命和饮水，并把`Time.timeScale`设为0。
- RestartButton的On Click绑定`RestartGame()`；GameScene已加入Scenes In Build。
- 重新开始时先恢复timeScale，再通过SceneManager重新加载当前场景。
- 验证：面板显示、暂停、按钮点击和全部初始状态恢复正常。

### 失败提示

- Canvas下独立创建MessageText，不放入会被隐藏的结算面板。
- `GameMessageUI.Show`填写并激活文字，用协程在两秒后隐藏。
- 重复请求时停止旧协程并重新计时。
- 验证：无样本显示“缺少任务物品：实验样本”，两秒后消失；成功流程不受影响。

## 8. JSON玩家状态存档

### 配置

- Gameplay新增SaveGame=F5、LoadGame=F9，并重新生成`PlayerInputActions.cs`。
- Player挂载`GameSaveSystem`，Known Items登记全部六种`ItemData`资产。
- 存档目录按项目相对路径计算为`SaveData/player-save.json`，并加入`.gitignore`。

### 验证

- F5写入生命、饮水和六格背包的物品ID与数量；F9可恢复全部六种物品。
- 背包恢复完成后只触发一次`InventoryChanged`，六格UI立即刷新。
- 新增能量饮料、绷带和强效急救剂后，拾取、使用、F5保存和F9还原均验证成功。
- 当前不保存玩家位置、敌人、世界拾取物状态、子弹和撤离完成状态。

## 9. 当前验证结论

### 小地图完整流程

- 已从安全出生点完成收集补给、战斗、取得实验样本和撤离结算的完整试玩。
- 敌人发现距离合适，当前战斗难度偏简单但可接受；饮水下降体感较快主要受地图流程较短影响。

### Profiler首轮Editor静止基线

- 在Play Mode目标下关闭Deep Profile，静止采集约10秒并选取中段普通帧。
- 选中帧CPU为4.63 ms，其中PlayerLoop 2.11 ms、EditorLoop 2.26 ms，GC Alloc为0 B。
- 内存曲线基本平稳；Total Committed Memory 1.30 GB、Managed Heap 474.2 MB、Profiler 297.2 MB。
- 内存数据包含Unity Editor和Profiler开销，只用于学习和场景内对比，不能作为最终Player内存结论。

### Profiler持续射击Editor对比

- 射击阶段第4492帧CPU 4.39 ms，PlayerLoop 2.12 ms、EditorLoop 2.08 ms，GC Alloc 0 B。
- 第4512帧CPU升至19.23 ms，但EditorLoop占16.95 ms、PlayerLoop仅2.09 ms；该尖峰不能归因于子弹逻辑。
- 两帧Game Objects均为65，内存曲线基本平稳，选中帧未显示对象池扩容或托管分配。
- 两个选中帧的Physics均为0 ms，尚不能用它们量化`Projectile.FixedUpdate`的物理成本。
- 补选第4438物理帧：CPU 5.13 ms、Physics约0.33 ms；11次`Projectile.FixedUpdate`合计约0.02 ms，GC Alloc 0 B。

### Profiler对象池扩容实验

- 临时将Initial Size从20改为1并停用RangedEnemy，扩容前Game Objects 43、Scene Objects 530、GC Alloc 0 B。
- 朝空处持续射击后Game Objects增至55、Scene Objects增至614，即新增12个GameObject和84个场景对象。
- 分配帧记录39次托管分配、合计9.6 KB；该帧CPU总时间4.89 ms，未出现明显CPU创建尖峰。
- Hierarchy搜索`Instantiate`只显示引擎异步实例化更新标记，未在当前非Deep Profile采样中直接定位调用源。
- 该对照证明容量不足会扩充并保留池对象；9.6 KB分配与扩容同时出现，但不把全部分配武断归因于单一函数。

### Development Player基线与状态UI优化

- Windows Development Build成功运行并通过Autoconnect Profiler连接`Fengfeng - Running`，采样中不再包含EditorLoop。
- 普通帧PlayerLoop约16.63 ms，其中约15.21 ms为60 Hz垂直同步等待，实际脚本与渲染工作较低；总内存约297 MB且曲线稳定。
- 旧状态UI在饮水显示整数变化的帧产生78 B、3次托管分配；Hierarchy定位链为`PlayerStatusUI.Update → GC.Alloc`。
- 保留“整数未变不刷新”的缓存判断，并把`$"..."`改为TMP `SetText("饮水：{0}/{1}", ...)`后，同条件复测选中帧PlayerLoop的GC Alloc从78 B降为0 B，周期红色分配尖峰消失。
- 优化只消除了中间临时字符串；约297.2 MB到296.5 MB的总内存变化属于正常采样波动，不能全部算作该优化节省。

### Development Player持续战斗复测

- 在独立Player中持续移动、向空地射击、命中远程敌人，并让远程与近战敌人同时行动。
- 修改前双方同帧命中时，2次`Projectile.OnTriggerEnter`合计产生约2.8 KB分配；其中`LogStringToConsole`约2.3 KB，来源为`Health.TakeDamage`中的生命值成功日志。
- 临时注释该高频`Debug.Log`并重新构建后，命中帧不再出现`LogStringToConsole`；首次相关路径为72 B，后续碰撞帧约48 B，分配降低97%以上。
- 首次较大样本包含`Mono.JIT`的48 B和约0.52 ms；后续相同碰撞路径复用已编译代码，`Projectile.OnTriggerEnter`约0.04 ms。
- 连续战斗前后总内存约296.6—297.2 MB、Managed Heap约2.5—2.6 MB，Game Objects保持68、Scene Objects保持551，未见对象池持续扩容或内存泄漏。
- 分配调用栈采集仍显示`Profiler.Callstack`测量标记；剩余24—72 B属于极小样本，不再为追求绝对0 B过度优化，正式Release构建不启用Development Profiler采集。

- 当前模块均通过Unity Play模式验证，无已知Console红色错误。
- Development Player静止基线、状态UI分配和持续战斗复测均已完成。

### Windows Release交付验证

- 生成非Development的Windows Release版本：`Builds/WindowsRelease/Running.exe`。
- Player Settings设为1280×720窗口化，便于测试和演示；Release包不连接Development Profiler。
- 在独立Release程序中完成移动、鼠标瞄准、双方战斗、拾取与六格背包、物品使用、F5/F9存读档、缺少任务物品提示、成功撤离和重新开始的全流程冒烟测试。
- Release存档实际生成在可执行文件同级的`SaveData/player-save.json`，证明`Application.dataPath/../SaveData`在构建后按预期工作。
- 测试无报错，当前本地MVP达到可独立运行和演示状态。

## 10. Health事件与TargetDummy倒地样品

### 脚本与场景配置

- `Health`增加`Damaged`、`HealthChanged`和`Died`事件，并在受伤、治疗、读取存档和死亡状态变化后发布对应通知。
- `PlayerStatusUI`改为订阅生命变化事件；启用时立即显示当前生命，`Update`只继续处理持续变化的饮水显示。
- `StatusPanel`配置`PlayerDamageFeedback`，引用Player上的`Health`和`HealthText`，用于受伤时短暂改变文字颜色。
- Player、ChaserEnemy、RangedEnemy与TargetDummy配置`DeactivateOnDeath`，共同订阅同对象Health的`Died`。
- 只在TargetDummy建立`VisualRoot`样品：根对象缩放归一，根BoxCollider保持碰撞尺寸；子对象保留模型缩放并移除重复碰撞。
- TargetDummy的`VisualRoot`引用已拖入，倒下角度为Z轴90度、位置偏移Y轴-0.5、倒下0.4秒、停留0.6秒，并把根碰撞体加入禁用列表。

### 已验证结果

- 进入游戏后生命UI直接显示正确当前值，不再出现初始`0/100`。
- 玩家受到伤害时生命文字闪红，连续受伤会重新计时，不会被旧协程提前恢复。
- 四类对象死亡处理均可触发；TargetDummy关闭碰撞后平滑倒下、停留并失活。
- 其他对象未配置`VisualRoot`，按设计使用立即失活后备流程，保留一个样品即可。
- 脚本编译与运行测试均无报错；暂未实现复活、死亡动画资源或对象池死亡重置。

## 11. TargetDummy对象池伤害飘字

### 配置

- 新增`DamageNumberEmitter`、`DamageNumberPool`和`DamageNumberView`三个脚本。
- 创建3D TextMeshPro的`DamageNumberView.prefab`，配置显示时长0.8秒、上升速度1，并由View自动朝向Main Camera。
- 场景新增`DamageNumberPool`，引用飘字Prefab，初始预热数量为8。
- TargetDummy添加`DamageNumberEmitter`，引用场景Pool，头顶位置偏移为`(0, 2, 0)`。

### 已验证结果

- 射击TargetDummy时会在目标头顶显示实际伤害数字，文字向上移动并渐隐，结束后失活回池。
- 多位数字由一个完整TMP View通过`SetText`显示，不按字符分别创建对象。
- Projectile回池和TargetDummy死亡不影响已经借出的飘字完成表现，因为View属于独立DamageNumberPool层级。
- Unity编译和Play Mode测试均成功，无Console报错。
- 本轮之后已补充玩家绿色治疗飘字；暴击、命中点、池容量上限或Profiler前后对比仍未实现。

## 12. 玩家治疗飘字与ChaserEnemy Animator占位样品

### 治疗飘字配置与验证

- `Health`增加`Healed(float actualHeal)`，只在治疗成功后发布实际恢复量。
- Player增加`HealingNumberEmitter`并引用场景中的`DamageNumberPool`，位置偏移为`(0, 2, 0)`。
- Pool增加绿色治疗颜色，DamageNumberView按伤害或治疗入口重置本次颜色和文字格式。
- 玩家残血使用医疗物品时显示绿色`+实际恢复量`并消耗一个；满血时不显示也不消耗，Play Mode验证成功。

### Animator配置与验证

- ChaserEnemy下建立`VisualRoot`胶囊子对象，删除子对象Collider，关闭根对象MeshRenderer；根对象继续保留CharacterController、AI、Health和攻击组件。
- 在`Assets/_Project/Animations/ChaserEnemy`创建Controller及Idle、Chase、Attack三个程序化占位Clip，VisualRoot添加Animator且关闭Apply Root Motion。
- Idle使用循环缩放表现呼吸，Chase使用循环本地Y位移表现移动起伏；Controller使用整数`State`完成0/1切换，自动跟随AI已经验证。
- Attack使用非循环本地正Z突进；`Attack` Trigger通过Any State进入，播放完成后按Exit Time返回Idle，手动Trigger验证成功。
- `ChaserEnemyAnimator`挂在VisualRoot并引用父级ChaserEnemy；OnEnable订阅StateChanged/Attacked，OnDisable退订，并在订阅后主动同步当前状态。
- ChaserEnemy在Attack状态也调用FaceTarget，玩家近距离绕行后攻击突进方向已经验证正确。
- `TryAttack成功 → Attacked → Animator.SetTrigger`自动播放已经验证；Attack Clip命中帧添加Animation Event，调用`ApplyAttackHit → ApplyPendingHit → Health.TakeDamage`，画面命中与扣血已经同步。

## 13. ChaserEnemy死亡边界、阵营与目标选择

### 死亡配置与修复

- ChaserEnemy的`DeactivateOnDeath`引用VisualRoot；死亡时关闭ChaserEnemy、MeleeEnemyAttack、Animator、ChaserEnemyAnimator和CharacterController，再执行视觉倒地与根对象失活。
- 首次配置后出现`CharacterController.Move called on inactive controller`；`ChaserEnemy.Update`现先检查自身`Health.IsDead`与控制器`enabled`，`MoveTowardsTarget`调用Move前再次检查控制器活动状态。
- 追击或攻击期间击杀敌人的倒地、UI展示与无警告流程已重新验证；敌人头顶血条按用户决定暂不实现。

### 阵营配置

- 新增`FactionMember`，Player根对象设置为Player，ChaserEnemy和RangedEnemy根对象设置为Enemy；Neutral按当前规则视为非敌对。
- Projectile发射时由`sourceHealth`取得发射者FactionMember，碰撞时取得目标FactionMember；双方都有阵营数据且不是敌对关系时跳过伤害，回池时清空来源Health与阵营引用。
- 同阵营子弹过滤代码已经接入；“友军位于弹道中间不扣血”仍保留为专项回归，未写成已验证结果。

### 最近敌对目标选择

- 新增`HostileTargetFinder`并配置到近战、远程敌人根对象，默认Target Layers为Everything。
- 搜索器用`Physics.OverlapSphereNonAlloc`把候选Collider写入固定`Collider[32]`，排除自己、死亡对象和非敌对阵营，再按`ClosestPoint`距离平方保留最近Health。
- ChaserEnemy和RangedEnemy移除固定`FindGameObjectWithTag("Player")`，默认每0.25秒刷新目标；AI保存搜索结果的Health和Transform后再追击、转向或攻击。
- Dummy临时设置为Player阵营时，敌人先选择较近Dummy，Dummy不再有效后重新选择玩家，Play Mode验证成功。
