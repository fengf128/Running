# 《撤离测试场》操作与作用记录

> 这里只记录“实际做了什么、为什么做、步骤是什么、产生了什么作用”。概念解释和技术区别统一记录在《知识.md》。未完成事项必须明确标注为待办。

## 1. 创建干净的 URP 项目

**为什么：** 最初误选了 `Universal 3D sample`，其中包含多个演示场景、XR、教程和示例脚本，不利于控制范围和说明代码归属。

**步骤：**

1. Unity Hub 选择编辑器 `2022.3.57f1c1`。
2. 选择 `Universal 3D` 核心模板，不选择 `Universal 3D sample`。
3. 在 `E:\Unity3D\Mission\Running` 重新创建项目。

**作用与结果：** 得到只有 Main Camera、Directional Light、Global Volume 和基础 URP 配置的空白项目；检查确认旧 Sample 内容没有残留。

## 2. 诊断编辑器布局警告

**为什么：** Unity 报告 `layout could not be fully loaded`，需要确认它是否影响项目。

**步骤：**

1. 只读检查 AppData 中的 Unity 布局文件。
2. 定位到旧布局仍引用 Tutorial、Cinemachine 和 Version Control 窗口。
3. Console 先 Clear，再切换 Tall/Wide；需要时另存为 `ExtractionDev`。

**作用与结果：** 确认该问题属于全局编辑器布局缓存，不属于项目代码；没有为消除提示而修改场景或 Gameplay 配置。

## 3. 清理 Plastic/UOS 并保留恢复备份

**为什么：** 项目路径已登记为 Plastic 工作区，Hub/Collab 又尝试重复加入；同时 UOS 不是该单机 Demo 所需依赖。项目决定使用 Git。

**步骤：**

1. 关闭 Unity 和 Unity Hub。
2. 将 `.plastic`、`ignore.conf`、UOS 生成脚本及修改前配置备份到：
   `E:\Unity3D\Mission\Running-setup-backup\2026-08-19-plastic-uos`
3. 从 manifest/lock 移除 `cn.unity.uos.launcher`、`com.unity.collab-proxy` 及只由 UOS 引入的 Newtonsoft JSON 条目。
4. 移除 `UNITY_UOS_SECURITY` 脚本宏。
5. 重新打开 Unity，检查包刷新和编译日志。

**作用与结果：** Plastic/UOS 没有被重新生成；manifest 和 lock 通过 JSON 校验；Editor.log 没有脚本编译错误。原配置可从备份恢复。

## 4. 配置 Unity Git 仓库

**为什么：** 保存可回退的开发历史，后续可关联 GitHub，并为作品提供过程证据。

**步骤：**

```powershell
cd "E:\Unity3D\Mission\Running"
git init -b main
git status
git add .
git commit -m "Unity项目基线"
git log --oneline -1
```

同时建立 `.gitignore`，忽略 Library、Temp、Logs、UserSettings、IDE 工程文件和构建产物。

**作用与结果：** 创建独立 `main` 仓库；首个提交为 `80cf9f2 Unity项目基线`。核对确认没有误跟踪生成目录、`.sln`、`.csproj` 或 Plastic 元数据。

## 5. 安装 Input System

**为什么：** 后续需要把 Move、Aim、Fire、Interact 等输入意图与具体键盘/鼠标按键解耦，并为手柄、改键和 Gameplay/UI 输入切换保留清晰结构。

**步骤：**

1. Unity 打开 `Window → Package Manager`。
2. 来源选择 `Unity Registry`。
3. 搜索官方 `Input System` 并点击 Install。
4. 按提示启用输入后端并重启 Unity。

**作用与结果：** 已安装 `com.unity.inputsystem 1.11.2`；manifest、packages-lock 和 Editor.log 验证通过。

## 6. 切换为新版输入后端

**为什么：** `Both` 会同时启用旧 Input Manager 和新 Input System。新项目不需要兼容旧代码，使用单一输入方案更容易维护和解释。

**步骤：**

1. 打开 `Edit → Project Settings → Player`。
2. 展开 `Other Settings → Configuration`。
3. 将 `Active Input Handling` 从 `Both` 改为 `Input System Package (New)`。
4. 按提示重启 Unity。

**作用与结果：** 已确认 `ProjectSettings.asset` 中 `activeInputHandler: 1`，后续代码只使用新版 Input System API。

## 7. 保持 C# Debug 模式

**为什么：** 当前处于开发和学习阶段，需要断点、变量检查和清晰的调试行为。

**步骤：** 右下角出现 `Switch to release mode` 时不切换，保持 `Mode: Debug`。

**作用与结果：** 保留 C# 调试能力；最终性能验证时再明确使用的 Editor/Player 配置。

## 8. 文档分工

**步骤与作用：**

- `创建项目.md`：记录进度、已完成/未完成状态、Bug 和证据路径。
- `知识.md`：记录概念、原理、区别和设计原因。
- `操作与作用.md`：记录实际步骤、为什么操作、作用和验证结果。

三份文档不得把计划中的功能写成已经完成。

