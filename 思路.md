# 思路

> 从需求推导代码：决定需要什么数据、找哪个对象、代码放在哪里以及整条功能链怎样连接。

## 1. 需求驱动六步法

以后不要从“该写哪个类”开始，而从效果开始：

```text
1. 我想实现什么结果？
2. 需要什么数据或能力？
3. 谁拥有这些数据或能力？
4. 谁负责当前业务规则？
5. 两个对象怎样连接和返回结果？
6. 数据变化后谁需要通知或显示？
```

例：撤离时显示Player生命。

```text
想显示生命
→ 需要当前生命和最大生命
→ Health拥有数据
→ ExtractionResultUI负责结算显示
→ ShowSuccess收到本次Player引用
→ player.GetComponent<Health>()取得组件
→ 读取公开属性并写入detailsText
```

调用链不是为了背诵，而是为了确定每段代码的落点。

## 2. 代码放置规则

先看需求句子的主语：

| 需求 | 负责人 |
|---|---|
| 玩家是否按键 | `PlayerController` |
| 附近谁最近 | `PlayerInteractor` |
| 子弹何时发射 | `PlayerShooter` |
| 子弹怎样复用 | `ProjectilePool` |
| 目标怎样扣血 | `Health` |
| 物品放哪一格 | `PlayerInventory` |
| 单格怎样增减 | `InventorySlot` |
| 没有样本能否撤离 | `ExtractionZone` |
| 结算怎样显示 | `ExtractionResultUI` |
| 临时文字何时隐藏 | `GameMessageUI` |

遇到状态数据再问：

1. 这是谁的数据？
2. 谁有权修改？
3. 当前脚本只是请求，还是实际执行？
4. 修改成功怎样返回？
5. 还有谁需要知道变化？

## 3. 连接方式怎么选

| 情况 | 选择 | 例子 |
|---|---|---|
| 已拿到运行时对象，要取它自身能力 | `对象.GetComponent<T>()` | 从本次Player取得Health |
| 两个不同场景对象固定连接 | `SerializeField`+Inspector | ExtractionZone引用ResultUI |
| 一个明确对象请求另一个立即执行 | 直接方法调用 | `messageUI.Show(...)` |
| 调用方需要知道成功或失败 | 返回值 | `TryAdd`、`HasItem`返回bool |
| 一个变化要通知多个可选系统 | 事件 | `InventoryChanged`通知UI |
| 多种具体对象需要同一调用形式 | 接口 | 拾取物和撤离点实现IInteractable |
| 多个对象共享同类稳定配置 | ScriptableObject | 世界物品和背包共用ItemData |

### 私有数据与受控入口

```text
外部说明“想做什么”
→ 数据拥有者的public方法检查规则
→ 满足条件才修改private状态
→ 返回成功或失败
→ 有变化时再通知监听者
```

例如`TryConsume`不是简单减一，还负责下标、空槽、归零清理和通知。外部不需要知道数组细节。

## 4. 系统职责地图

### 玩家与战斗

| 脚本 | 负责 | 不负责 |
|---|---|---|
| `PlayerController` | 读取输入、移动、瞄准和调度 | 子弹、背包和效果规则 |
| `PlayerShooter` | 冷却与发射请求 | 子弹移动和伤害 |
| `ProjectilePool` | 创建、取出、归还子弹 | 决定何时开火 |
| `Projectile` | 自身移动、命中、寿命 | 管理其他子弹和生命 |
| `Health` | 生命、伤害、治疗、死亡 | 判断命中和选择药品 |
| `ChaserEnemy` | 检测、追击、近战 | 通用生命和玩家输入 |
| `RangedEnemy` | 检测、转向、射击请求 | 对象池内部管理 |

### 交互、物品与背包

| 脚本 | 负责 | 不负责 |
|---|---|---|
| `PlayerInteractor` | 范围检测、过滤接口、选最近目标 | 各目标具体行为 |
| `IInteractable` | 规定共同调用形式 | 保存状态和业务实现 |
| `WorldItemPickup` | 请求添加自身物品，成功后隐藏 | 选择槽位和使用效果 |
| `ItemData` | 一类物品的共享配置 | 数量和拾取行为 |
| `InventorySlot` | 一格物品、数量和本格操作 | 选择第几格和通知UI |
| `PlayerInventory` | 管理六格、查询/修改、发布变化 | 饮水、治疗、撤离和显示 |
| `PlayerItemUser` | 选择物品效果，成功后请求消耗 | 直接改槽位和生命字段 |

### 撤离与UI

| 脚本 | 负责 | 不负责 |
|---|---|---|
| `ExtractionZone` | 撤离条件和本局完成状态 | 遍历背包内部和拼文字 |
| `ExtractionResultUI` | 结算显示、暂停和重开 | 判断撤离条件 |
| `GameMessageUI` | 显示临时消息并隐藏 | 决定提示内容 |
| `InventoryUI` | 读取六格并按下标分配 | 修改背包和拼单格文字 |
| `InventorySlotView` | 显示一格 | 主动寻找背包 |

## 5. 核心功能链

### 射击

```text
鼠标左键
→ PlayerController请求PlayerShooter
→ PlayerShooter检查冷却
→ ProjectilePool取出并激活子弹
→ Projectile移动和检测命中
→ Health.TakeDamage修改目标生命
→ 命中或超时后归还Pool
```

### 拾取与背包UI

```text
E键
→ PlayerInteractor找到最近IInteractable
→ 实际WorldItemPickup执行Interact(Player)
→ 从Player取得PlayerInventory
→ TryAdd先堆叠、再找空格
→ InventorySlot修改item和quantity
→ InventoryChanged.Invoke
→ InventoryUI读取Slots
→ 六次循环把每格交给对应SlotView
→ TryAdd返回true后世界物品停用
```

### 使用消耗品

```text
数字键
→ PlayerController把按键转换为槽位下标
→ PlayerItemUser通过TryGetItem取得ItemData
→ Water请求Hydration.TryRestore；Medical请求Health.TryHeal
→ 效果返回true后PlayerInventory.TryConsume
→ 槽位减一并触发InventoryChanged
→ UI重新读取并显示
```

### 撤离失败

```text
E键
→ PlayerInteractor调用最近ExtractionZone
→ ExtractionZone请求PlayerInventory.HasItem
→ 返回false，不修改背包、不触发InventoryChanged
→ ExtractionZone直接调用GameMessageUI.Show
→ MessageText激活
→ 协程等待两秒后停用；重复请求会重新计时
```

### 撤离成功与重开

```text
HasItem返回true
→ ExtractionZone把completed设为true
→ 直接调用ExtractionResultUI.ShowSuccess
→ UI从Player读取Health和Hydration
→ 显示结算并把timeScale设为0
→ Button.OnClick调用RestartGame
→ 恢复timeScale并重载当前场景
```

## 6. 技术选择与取舍

- 统一交互用接口：调用者需要处理拾取物和撤离点；若只有一个固定目标，直接调用更简单。
- 背包UI用事件：数据变化频率低且UI不应每帧轮询；当前事件不携带索引，六格全部刷新足够便宜。
- 撤离UI用直接引用：目前是一对一；以后音效、存档、成就都监听撤离时再引入事件。
- 物品用ScriptableObject：相同定义可共享并在Inspector调参；数量等运行状态仍保存在槽位实例。
- 子弹用对象池：降低频繁创建销毁和GC；需要额外处理预热、扩容和状态重置。
- 敌人暂不用NavMesh：开阔小地图的直接移动足够；代价是不能绕障。

## 7. JSON存档思路

### 职责

| 对象 | 只负责 | 不负责 |
|---|---|---|
| `PlayerController` | 识别F5/F9并发出请求 | JSON和文件路径 |
| `GameSaveSystem` | 汇总数据、JSON转换、文件读写、ID映射 | 加血、饮水和背包规则 |
| `GameSaveData` | 临时装载可序列化字段 | 游戏行为 |
| `Health/Hydration/Inventory` | 保管并受控恢复自己的状态 | 文件读写 |

### 保存链

```text
F5
→ PlayerController调用GameSaveSystem.SaveGame
→ CreateSaveData读取Health、Hydration和六格Slots
→ 将ItemData引用转成itemId，组装独立的GameSaveData快照
→ JsonUtility.ToJson
→ File.WriteAllText写入SaveData/player-save.json
```

### 读取链

```text
F9
→ 读取文件并FromJson得到GameSaveData
→ knownItems按itemId找回ItemData资产
→ 请求Health、Hydration和Inventory恢复
→ Inventory恢复六格后Invoke一次
→ InventoryUI重新读取并显示
```

- 保存ID而不是资产引用，使JSON是普通文本，也能跨运行过程重新建立引用。
- `CreateSaveData`只读取并组装快照；JSON转换和文件写入由`SaveGame`继续协调。
- 三种物品使用数组线性查找足够简单；物品很多时再考虑字典。
- 当前只保存玩家状态，不保存世界状态，因此不能称为完整关卡存档。

## 8. 面试复述模板

```text
我先从需求确定所需数据和数据拥有者，再按输入、规则、数据和显示拆分职责。
外部脚本通过public入口请求数据拥有者查询或修改，成功结果用返回值交回；
数据变化后再通过事件通知UI。不同交互对象用IInteractable统一调用，
共享物品配置使用ScriptableObject，子弹通过对象池复用。
当前方案针对小型单机Demo控制复杂度，并保留了事件、导航和效果策略的扩展点。
```
