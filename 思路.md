# 思路

> 从需求推导代码：决定需要什么数据、找哪个对象、代码放在哪里以及整条功能链怎样连接。

## 1. 需求驱动六步法

以后不要从“该写哪个类”开始，而从效果开始：

```text
1. 我想实现什么结果？
2. 需要什么数据或能力？
3. 谁拥有这些数据或能力？
4. 谁负责当前业务规则？
5. 两个对象怎样连接和返回结果？
6. 数据变化后谁需要通知或显示？
```

例：撤离时显示Player生命。

```text
想显示生命
→ 需要当前生命和最大生命
→ Health拥有数据
→ ExtractionResultUI负责结算显示
→ ShowSuccess收到本次Player引用
→ player.GetComponent<Health>()取得组件
→ 读取公开属性并写入detailsText
```

调用链不是为了背诵，而是为了确定每段代码的落点。

## 2. 代码放置规则

先看需求句子的主语：

| 需求 | 负责人 |
|---|---|
| 玩家是否按键 | `PlayerController` |
| 附近谁最近 | `PlayerInteractor` |
| 子弹何时发射 | `PlayerShooter` |
| 子弹怎样复用 | `ProjectilePool` |
| 目标怎样扣血 | `Health` |
| 物品放哪一格 | `PlayerInventory` |
| 单格怎样增减 | `InventorySlot` |
| 没有样本能否撤离 | `ExtractionZone` |
| 结算怎样显示 | `ExtractionResultUI` |
| 临时文字何时隐藏 | `GameMessageUI` |

遇到状态数据再问：

1. 这是谁的数据？
2. 谁有权修改？
3. 当前脚本只是请求，还是实际执行？
4. 修改成功怎样返回？
5. 还有谁需要知道变化？

## 3. 连接方式怎么选

| 情况 | 选择 | 例子 |
|---|---|---|
| 已拿到运行时对象，要取它自身能力 | `对象.GetComponent<T>()` | 从本次Player取得Health |
| 两个不同场景对象固定连接 | `SerializeField`+Inspector | ExtractionZone引用ResultUI |
| 一个明确对象请求另一个立即执行 | 直接方法调用 | `messageUI.Show(...)` |
| 调用方需要知道成功或失败 | 返回值 | `TryAdd`、`HasItem`返回bool |
| 一个变化要通知多个可选系统 | 事件 | `InventoryChanged`通知UI |
| 多种具体对象需要同一调用形式 | 接口 | 拾取物和撤离点实现IInteractable |
| 多个对象共享同类稳定配置 | ScriptableObject | 世界物品和背包共用ItemData |

### 私有数据与受控入口

```text
外部说明“想做什么”
→ 数据拥有者的public方法检查规则
→ 满足条件才修改private状态
→ 返回成功或失败
→ 有变化时再通知监听者
```

例如`TryConsume`不是简单减一，还负责下标、空槽、归零清理和通知。外部不需要知道数组细节。

## 4. 系统职责地图

### 玩家与战斗

| 脚本 | 负责 | 不负责 |
|---|---|---|
| `PlayerController` | 读取输入、移动、瞄准和调度 | 子弹、背包和效果规则 |
| `PlayerShooter` | 冷却与发射请求 | 子弹移动和伤害 |
| `ProjectilePool` | 创建、取出、归还子弹 | 决定何时开火 |
| `Projectile` | 自身移动、命中、寿命 | 管理其他子弹和生命 |
| `Health` | 生命、伤害、治疗、死亡 | 判断命中和选择药品 |
| `ChaserEnemy` | 根据距离决定Idle/Chase/Attack并执行追击 | 近战伤害、冷却和通用生命 |
| `RangedEnemy` | 检测范围、目标转向和攻击请求 | 发射冷却、对象池内部和子弹伤害 |
| `IEnemyAttack` | 规定`TryAttack(target)`统一入口 | 保存伤害、冷却和具体实现 |
| `MeleeEnemyAttack` | 检查冷却并请求目标Health扣血 | 决定AI状态和移动 |
| `RangedEnemyAttack` | 检查冷却、计算发射方向并请求对象池 | 子弹飞行、碰撞和命中伤害 |

### 交互、物品与背包

| 脚本 | 负责 | 不负责 |
|---|---|---|
| `PlayerInteractor` | 范围检测、过滤接口、选最近目标 | 各目标具体行为 |
| `IInteractable` | 规定共同调用形式 | 保存状态和业务实现 |
| `WorldItemPickup` | 请求添加自身物品，成功后隐藏 | 选择槽位和使用效果 |
| `ItemData` | 一类物品的共享配置 | 数量和拾取行为 |
| `InventorySlot` | 一格物品、数量和本格操作 | 选择第几格和通知UI |
| `PlayerInventory` | 管理六格、查询/修改、发布变化 | 饮水、治疗、撤离和显示 |
| `PlayerItemUser` | 选择物品效果，成功后请求消耗 | 直接改槽位和生命字段 |

### 撤离与UI

| 脚本 | 负责 | 不负责 |
|---|---|---|
| `ExtractionZone` | 撤离条件和本局完成状态 | 遍历背包内部和拼文字 |
| `ExtractionResultUI` | 结算显示、暂停和重开 | 判断撤离条件 |
| `GameMessageUI` | 显示临时消息并隐藏 | 决定提示内容 |
| `InventoryUI` | 读取六格并按下标分配 | 修改背包和拼单格文字 |
| `InventorySlotView` | 显示一格 | 主动寻找背包 |
| `PlayerStatusUI` | 读取并显示实时生命和饮水 | 创建Player或修改状态 |

## 5. 核心功能链

### 射击

```text
鼠标左键
→ PlayerController请求PlayerShooter
→ PlayerShooter检查冷却
→ ProjectilePool取出并激活子弹
→ Projectile移动和检测命中
→ Health.TakeDamage修改目标生命
→ 命中或超时后归还Pool
```

### 敌人状态机与攻击策略

```text
ChaserEnemy每帧计算目标距离平方
→ DecideState只返回本帧应处于Idle、Chase或Attack
→ ChangeState只在状态真正变化时更新currentState
→ ExecuteState根据currentState执行待机、移动或攻击请求
→ Attack分支通过IEnemyAttack.TryAttack(target)统一调用
→ 实际组件是MeleeEnemyAttack时扣血，是RangedEnemyAttack时发射子弹
```

- 状态机决定“现在做什么”，攻击策略决定“具体怎么攻击”。
- 接口只规定共同入口；伤害、冷却、发射点和对象池引用保存在具体策略组件。
- `GetComponent<IEnemyAttack>()`查找同一敌人上已有的接口实现实例，不创建新组件。
- 同一句`TryAttack`根据实际实例执行不同方法体，属于多态分派；把可替换攻击行为独立为组件，属于策略模式。
- `TryAttack`返回`true`表示本次攻击成功开始：近战暂存目标并触发动画，远程成功取得并发射子弹；近战真正扣血延后到Animation Event命中帧。冷却未结束、目标无效或对象池失败时返回`false`。

### 拾取与背包UI

```text
E键
→ PlayerInteractor找到最近IInteractable
→ 实际WorldItemPickup执行Interact(Player)
→ 从Player取得PlayerInventory
→ TryAdd先堆叠、再找空格
→ InventorySlot修改item和quantity
→ InventoryChanged.Invoke
→ InventoryUI读取Slots
→ 六次循环把每格交给对应SlotView
→ TryAdd返回true后世界物品停用
```

### 使用消耗品

```text
数字键
→ PlayerController把按键转换为槽位下标
→ PlayerItemUser通过TryGetItem取得ItemData
→ Water请求Hydration.TryRestore；Medical请求Health.TryHeal
→ 效果返回true后PlayerInventory.TryConsume
→ 槽位减一并触发InventoryChanged
→ UI重新读取并显示
```

### 增加同类物品

```text
创建ItemData并填写ID、类型、效果值和堆叠上限
→ 世界物体引用该资产，继续复用WorldItemPickup
→ 拾取后PlayerItemUser按ItemType选择饮水或治疗入口
→ effectAmount决定具体效果强度
→ 在Known Items登记资产，使JSON中的ID能还原为ItemData引用
```

这里没有为能量饮料、绷带和强效急救剂各写一套拾取脚本，因为它们的行为规则相同，变化的是配置数据。

### 实时状态显示

```text
Health和PlayerHydration拥有实际数据
→ PlayerStatusUI通过Inspector引用两个现有组件实例
→ Update读取当前值并转换为整数
→ 与上次显示值相同：直接结束本项，不修改文字
→ 与上次显示值不同：用SetText把数值填入固定模板
→ TMP在内部可复用字符缓冲区中更新显示
```

饮水每帧变化，不必再增加每帧触发的事件；第一层缓存判断减少更新次数，第二层`SetText`减少必须更新时的临时字符串分配。

### 撤离失败

```text
E键
→ PlayerInteractor调用最近ExtractionZone
→ ExtractionZone请求PlayerInventory.HasItem
→ 返回false，不修改背包、不触发InventoryChanged
→ ExtractionZone直接调用GameMessageUI.Show
→ MessageText激活
→ 协程等待两秒后停用；重复请求会重新计时
```

### 撤离成功与重开

```text
HasItem返回true
→ ExtractionZone把completed设为true
→ 直接调用ExtractionResultUI.ShowSuccess
→ UI从Player读取Health和Hydration
→ 显示结算并把timeScale设为0
→ Button.OnClick调用RestartGame
→ 恢复timeScale并重载当前场景
```

## 6. 技术选择与取舍

- 统一交互用接口：调用者需要处理拾取物和撤离点；若只有一个固定目标，直接调用更简单。
- 敌人攻击用策略组件：AI判断和攻击实现已经出现两种变化方向；若始终只有一种固定攻击，保留在AI脚本中会更简单。
- 背包UI用事件：数据变化频率低且UI不应每帧轮询；当前事件不携带索引，六格全部刷新足够便宜。
- 撤离UI用直接引用：目前是一对一；以后音效、存档、成就都监听撤离时再引入事件。
- 物品用ScriptableObject：相同定义可共享并在Inspector调参；数量等运行状态仍保存在槽位实例。
- 子弹用对象池：降低频繁创建销毁和GC；需要额外处理预热、扩容和状态重置。
- 敌人暂不用NavMesh：开阔小地图的直接移动足够；代价是不能绕障。

## 7. JSON存档思路

### 职责

| 对象 | 只负责 | 不负责 |
|---|---|---|
| `PlayerController` | 识别F5/F9并发出请求 | JSON和文件路径 |
| `GameSaveSystem` | 汇总数据、JSON转换、文件读写、ID映射 | 加血、饮水和背包规则 |
| `GameSaveData` | 临时装载可序列化字段 | 游戏行为 |
| `Health/Hydration/Inventory` | 保管并受控恢复自己的状态 | 文件读写 |

### 保存链

```text
F5
→ PlayerController调用GameSaveSystem.SaveGame
→ CreateSaveData读取Health、Hydration和六格Slots
→ 每格新建InventorySlotSaveData并复制itemId和quantity
→ 组装与运行状态独立的GameSaveData快照
→ JsonUtility.ToJson得到字符串
→ File.WriteAllText写入SaveData/player-save.json
```

### 读取链

```text
F9
→ File.ReadAllText取得JSON字符串
→ FromJson新建GameSaveData快照实例
→ 先检查快照、槽位数组和六格长度
→ 建立ItemData[]与int[]两个临时数组
→ FindItem只按itemId找回已有ItemData资产引用，数量单独复制
→ Inventory按相同下标恢复六格，再恢复Health和Hydration
→ Inventory恢复完成后Invoke一次
→ InventoryUI重新读取并显示
```

- 保存ID而不是资产引用，使JSON是普通文本，也能跨运行过程重新建立引用。
- `CreateSaveData`只读取并组装快照；快照保存复制值，不持续引用运行中的槽位状态。
- `FindItem`只负责“ID→资产引用”，数量范围由槽位恢复入口处理。
- 恢复前先校验全部数组；失败返回`false`属于正常流程，不会进入`catch`。
- 最初采用数组线性查找；学习配置索引时改为在Awake遍历Known Items，建立`Dictionary<string, ItemData>`。键是唯一itemId，值是已有资产引用，不是背包数量，也不会复制资产。读档使用TryGetValue，找不到返回null；重复ID报错并保留首项。2026-09-10用户确认F5/F9验证成功。
- 配置导入与读档不是同一步：编辑器先将CSV配置按ID写入已有ItemData资产；运行时仍读取资产，读档仍按存档ID找资产并恢复数量。更新资产字段而不删除重建，保留GUID及场景/Prefab引用。第一版只更新已有ID，不生成新物品，不自动导入，不属于热更新。
- 当前只保存玩家状态，不保存世界状态，因此不能称为完整关卡存档。

## 8. Profiler验证思路

```text
静止基线
→ 移动/瞄准
→ 持续射击并复用对象池
→ 必要时制造对象池扩容
→ 在相同场景和采样方式下比较CPU、GC和内存趋势
→ 最后用Development Build复测
```

- 先提出可验证假设：20枚预热子弹在正常射速和2秒寿命下应能循环复用。
- 静止、射击和扩容分开测，避免不知道变化来自哪个操作。
- 看普通帧判断稳定成本，看尖峰定位偶发卡顿，看内存曲线判断是否持续增长。
- 射击时CPU适度增加可能只是活跃子弹在移动和碰撞；对象池减少的是创建销毁，不消除玩法计算。
- 发现尖峰先看时间属于PlayerLoop还是EditorLoop，再向下找具体脚本或系统。
- GameObject数量和内存稳定可支持“正在复用”的判断，但最终仍需Player环境复测。
- 扩容实验只改对象池容量并停用远程敌人：对象数从43增至55，验证Queue为空时会创建并长期保留新池对象。
- Editor数据只用于阶段对比；最终作品证据取Player中的可复现数据。
- Development Player中先发现状态UI周期产生78 B分配，再沿Hierarchy定位到`PlayerStatusUI.Update`；只替换字符串生成方式后同条件降为0 B，形成“发现→定位→最小修改→复测”的完整证据链。
- 持续战斗时再次使用相同方法：命中帧从`Projectile.OnTriggerEnter`向下定位到`LogStringToConsole`，再追到`Health.TakeDamage`的成功日志；只注释该日志后重建复测，分配由2.8 KB降到48—72 B，对象数和内存仍稳定。
- Profiler父节点表示整条调用链的总成本：`Projectile.OnTriggerEnter`显示43 ms不代表碰撞本身用了43 ms，继续向下发现大部分来自日志和首次JIT。

### 从性能验证到交付验证

```text
Development Player
→ 连接Profiler，定位CPU、GC Alloc和内存变化来自哪里
→ 根据证据做最小修改并在相同条件下复测
→ 得到“为什么变好”的性能证据

Windows Release
→ 不连接开发Profiler，以实际发布配置运行
→ 从开始到撤离逐条执行玩家操作
→ 验证功能、输入、UI、存档路径和重新开始都能工作
→ 得到“玩家拿到后能正常玩”的交付证据
```

Development构建回答“问题在哪里、修改有没有效果”；Release构建回答“最终交付版本能不能完整运行”。两种测试职责不同，不能互相替代。

## 9. 面试复述模板

```text
我先从需求确定所需数据和数据拥有者，再按输入、规则、数据和显示拆分职责。
外部脚本通过public入口请求数据拥有者查询或修改，成功结果用返回值交回；
数据变化后再通过事件通知UI。不同交互对象用IInteractable统一调用，
共享物品配置使用ScriptableObject，子弹通过对象池复用。
当前方案针对小型单机Demo控制复杂度，并保留了事件、导航和效果策略的扩展点。
```

## 10. 下一阶段学习落地方法

每个新知识都按同一条链推进：

```text
先从当前已完成系统找到真实问题
→ 复习与问题直接相关的旧知识
→ 回答数据属于谁、谁请求、谁执行、谁通知、谁显示
→ 只设计一个可验证的最小版本
→ 编写代码并完成Inspector配置
→ 测试成功、失败、边界和重复触发
→ 必要时使用Profiler做前后对比
→ 最后用自己的话复述逻辑链和取舍
```

敌人状态机和攻击策略已经完成最小实现与运行验证：代码状态机决定敌人“做什么”，攻击策略决定“怎么攻击”，Health拥有生命规则。现已用程序化占位Animation Clip和Animator Controller补充表现，不依赖外部骨骼素材；玩法根对象与VisualRoot分离，避免动画修改CharacterController和AI坐标。

战斗反馈事件已经完成最小实现与运行验证：Health在数据改变后按语义发布`Damaged`、`HealthChanged`和`Died`，生命UI、受伤闪红与死亡处理各自按需订阅。A*目前只作为开放表、关闭表和`F=G+H`的寻路概念储备；当前开阔小地图没有绕障需求，不误写成已实现功能。

## 11. Health事件、受伤反馈与死亡流程

这次仍按“数据属于谁、谁请求、谁执行、谁通知、谁表现”拆分职责：

```text
子弹或近战攻击请求TakeDamage
→ Health校验并计算实际伤害
→ Health修改CurrentHealth
→ Damaged通知受伤反馈
→ HealthChanged通知生命UI
→ 首次归零时Died通知死亡处理
```

- `Health`拥有生命数据和生命规则，攻击脚本只是调用者，不直接控制UI、颜色或死亡动画。
- `PlayerStatusUI`订阅`HealthChanged`，生命显示不再每帧轮询；饮水仍因持续变化而在`Update`中按整数缓存刷新。
- UI启用时主动读取一次当前生命，Health在`Awake`初始化后也发布一次变化，避免跨对象生命周期先后不确定造成初始`0/100`。
- `PlayerDamageFeedback`订阅`Damaged`。连续受伤时先停止旧闪红协程再启动新的，避免旧流程过早把颜色恢复。
- `DeactivateOnDeath`订阅`Died`。未配置视觉子对象时立即失活；配置后先关闭指定行为和碰撞，再逐帧倒下、停留并失活。

程序化倒地的时间链：

```text
累计elapsed += Time.deltaTime
→ progress = elapsed / fallDuration
→ 插值视觉子对象的局部旋转和位置
→ yield return null，把控制权交回Unity并等下一帧
→ 到达最终姿态后等待remainDuration
→ 根对象SetActive(false)
```

协程句柄在两个出口清空：正常执行完时清空，表示已经结束；对象被外部停用时先停止再清空，表示流程被中断。当前倒地是一次性死亡流程，若以后加入复活或对象池复用，还要恢复生命、行为、碰撞和视觉姿态。

## 12. 对象池伤害飘字与项目场景排错

已验证调用链：

```text
Projectile命中并请求TakeDamage
→ Health计算actualDamage并发布Damaged
→ DamageNumberEmitter订阅并补充目标头顶位置
→ DamageNumberPool从Queue借出一个DamageNumberView
→ View重置文字、位置、颜色、时间、旋转和缩放
→ View显示、上升、渐隐
→ 完成后失活并Enqueue回池
```

- `Health`是生命数据和规则拥有者；Emitter不是数据拥有者，只把伤害结果转成表现请求。
- 对象池保存的是完整TMP文字对象，不是单独数字字符；显示`125`仍只借出一个View并使用`SetText`写入内容。
- Pool负责预热、借出、回收和库存为空时扩容；View负责具体表现与复用状态重置。
- View属于独立Pool层级，因此Projectile回池或TargetDummy失活后，已出现的数字仍可完成自己的表现。
- 当前预热8个View，只在TargetDummy配置Emitter并完成Play验证；容量上限与压力测试尚未完成。

项目场景排错使用“第一处异常”方法：

```text
画出调用链
→ 写出每一步预期调用次数
→ 找到第一次从1变成多次或第一次出现错误状态的位置
→ 只检查该位置和上一步的职责边界
→ 修复后重复同一场景验证
```

例如一次命中中`TakeDamage`为1次、`HandleDamaged`为3次，应优先检查重复订阅或重复Emitter；若借出次数为1但文字半透明，应检查`DamageNumberView.Show`是否重置颜色和时间。对象反复启用后数量越来越多，尤其要检查`OnEnable/OnDisable`是否成对。

接口的提取条件不是“两个对象功能相似”，而是“存在一个调用者需要在不知道具体类型时使用统一入口”。`IInteractable`让PlayerInteractor统一调用世界物品、撤离点和未来治疗站；医疗包与治疗站目前由不同调用链触发，只需共同请求Health，不需要为了回血强行增加新接口。

绿色治疗飘字和`Healed(actualHeal)`已经实现并通过Play Mode验证：`Health`裁定实际恢复量，`PlayerItemUser`根据`TryHeal`返回值决定是否消耗，`HealingNumberEmitter`只负责把成功通知转为绿色表现请求。治疗站、暴击信息和准确命中点仍只是设计讨论，尚未实现。

## 13. 脚本状态机与Animator状态机

ChaserEnemy的动画采用两条职责链：

```text
距离变化
→ ChaserEnemy决定Idle/Chase/Attack
→ 状态真正变化时发布StateChanged
→ ChaserEnemyAnimator设置Animator的State整数
→ Animator Controller切换Idle/Chase表现
```

```text
处于攻击范围
→ IEnemyAttack.TryAttack检查目标和冷却
→ 只有返回true才发布Attacked
→ ChaserEnemyAnimator设置Attack Trigger
→ Any State进入一次Attack动画
```

`State`是持续数据：0、1、2分别表示Idle、Chase、Attack范围状态；`Attack`是一次性Trigger，表示刚刚成功执行一次攻击。不能只用`State = 2`代替攻击触发，否则Animator无法知道冷却在哪一帧结束。

`Animator.StringToHash("State")`只把参数名字转换成可复用的固定整数编号，不会得到0到2。`Animator.SetInteger(StateParameter, value)`的第一个参数负责指出“修改哪个Animator参数”，第二个参数才是写入其中的状态值。`readonly`保护的是编号字段，不能阻止Animator对象内部的参数值发生变化。

动画只修改`VisualRoot`。AI在Chase和Attack状态调用`FaceTarget`旋转根对象，视觉子对象继承朝向，再由Attack Clip沿本地正Z方向突进。这样“目标方向”属于AI，“向前突进的视觉变化”属于Animator。

当前Idle/Chase自动切换、成功攻击自动Trigger和攻击朝向已经验证。近战攻击已拆成“开始攻击”和“命中结算”：`TryAttack`检查目标与冷却并暂存`pendingTargetHealth`，`Attacked`触发Attack Clip，Animation Event在命中帧调用`ApplyPendingHit`后才进入`Health.TakeDamage`。该完整链路已通过Play Mode验证。

## 14. 阵营与最近敌对目标选择

```text
AI每隔targetRefreshInterval请求一次目标
→ HostileTargetFinder用OverlapSphereNonAlloc取得范围候选
→ 通过碰撞体向父级查找Health
→ 排除自己、死亡目标、同阵营和中立阵营
→ 用Collider.ClosestPoint和距离平方比较
→ 返回最近目标的Health
→ AI保存Health与Transform，再决定状态和执行行为
```

- `FactionMember`只保存Player、Enemy、Neutral身份；`Health`仍只拥有生命数据与规则。
- 近战与远程AI不再固定依赖`FindGameObjectWithTag("Player")`，而是复用同一个目标搜索组件。
- 目标搜索和E交互使用同一类“候选、筛选、比较、执行”结构：交互筛选`IInteractable`，AI筛选存活的敌对阵营。
- `Physics.OverlapSphereNonAlloc`是球形检测方法，预先创建的`Collider[]`只是接收结果的可复用缓冲区；方法返回本次写入的有效数量。
- 默认每0.25秒重新搜索，而不是每帧全量检测；目标死亡后会被`IsDead`过滤，再选择其他最近敌对目标。
- Dummy临时设为Player阵营后，敌人先选择较近Dummy、其后改选玩家的流程已通过Play Mode验证。同阵营子弹过滤已接入，友军挡枪仍保留为专项回归。

## 15. 学习迁移方式

每项技术保留一个真实项目主线和最多一个近距离变式，随后切换新知识；旧知识通过新模块自然复习。练习顺序使用“现象/需求 → 请求者 → 规则拥有者 → 通知者 → 表现者 → 预期调用次数 → 第一处异常 → 受控验证”，最后形成简短面试复述，不围绕同一业务反复换皮。
