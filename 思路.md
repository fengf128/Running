# 思路

> 只记录系统怎么拆、代码放哪里、功能链怎么走；概念定义见 `知识.md`，实际操作见 `操作与作用.md`，学习检查见 `通过项目理解.md`。

## 1. 总牵引线

```text
输入 → 调度请求 → 功能规则 → 数据查询/修改 → 结果返回 → 事件通知 → UI显示
```

- 输入只表达玩家意图，不直接修改各系统数据。
- 功能脚本判断这次行为能否发生，并向数据拥有者请求操作。
- 数据拥有者保管私有状态，通过 public 方法提供受控入口。
- UI 读取结果并显示，不决定业务是否成功。

## 2. 判断代码放在哪里

先找需求句子的主语：

- “玩家是否按了 E”：`PlayerController`。
- “附近谁最近”：`PlayerInteractor`。
- “没有样本能否撤离”：`ExtractionZone`。
- “六格中怎样找样本”：`PlayerInventory`。
- “这一格怎样减一”：`InventorySlot`。
- “这一格怎样显示”：`InventorySlotView`。

再问五个问题：

1. 这是谁的数据？
2. 谁有权修改？
3. 当前脚本是请求者还是执行者？
4. 数据改变后谁通知？
5. 最后谁显示？

### 私有数据与受控入口

```text
外部脚本提出“想做什么”
→ 数据所属类的 public 方法检查规则
→ 规则满足才修改 private 数据
→ 用返回值报告成功或失败
```

- `private` 字段由所属类保管，外部不能随意改。
- public 方法表示外部被允许进行的操作，例如 `TryAdd`、`TryConsume`、`HasItem`。
- 规则不是“方法名字”，而是方法内部的条件和状态变化。
- `TryConsume` 不只做减一，还统一检查下标、空槽、归零清理和变化通知。
- 只给外部真正需要的入口；内部辅助方法仍可保持 private。

这样修改规则时只改数据所属类，外部脚本不需要知道数组和字段细节。

## 3. 四种工具分别解决什么

| 工具 | 解决的问题 | 本项目例子 |
|---|---|---|
| Input bool | 玩家这一帧是否发出意图 | E、鼠标左键、数字键 |
| `IInteractable` | 同一调用者怎样统一调用不同目标 | 拾取物、撤离点都响应 E |
| `ScriptableObject` | 共享配置怎样外置并重复引用 | 多个对象共用同一 `ItemData` |
| 封装 | 谁保管数据，外部怎样受控查询或修改 | `slots` 私有，外部调用背包方法 |
| 事件 | 数据改变后怎样通知未知数量的监听者 | `InventoryChanged` 通知 UI |

这些工具不能互相替代：接口统一“怎么调用”，封装控制“怎么改数据”，事件负责“改完后通知”，ScriptableObject保存共享配置。

## 4. 脚本职责边界

| 脚本 | 只负责 | 不负责 |
|---|---|---|
| `PlayerController` | 读取输入并调度 | 子弹、背包、物品效果的具体规则 |
| `TopDownCamera` | 跟随并看向 Player | 玩家移动与瞄准 |
| `PlayerShooter` | 射击冷却和请求子弹 | 子弹移动、伤害与回收 |
| `ProjectilePool` | 创建、取出和回收子弹 | 决定何时开火和目标扣血 |
| `Projectile` | 自身移动、命中、寿命和归还 | 管理其他子弹和目标生命 |
| `Health` | 当前生命、扣血、治疗和死亡 | 子弹如何命中、药品如何选择 |
| `ChaserEnemy` | 检测、追击和近战攻击 | 通用生命和玩家输入 |
| `RangedEnemy` | 检测、转向和射击请求 | 子弹池内部管理 |
| `PlayerInteractor` | 范围检测、过滤接口、选择最近目标 | 各目标的具体交互行为 |
| `IInteractable` | 规定交互名称和调用形式 | 保存状态和实现具体行为 |
| `WorldItemPickup` | 请求把自身物品加入背包，成功后隐藏 | 背包放哪格和物品使用效果 |
| `ItemData` | 保存一类物品的共享配置 | 槽位数量和拾取行为 |
| `InventorySlot` | 保存并操作一格物品和数量 | 选择第几格和通知 UI |
| `PlayerInventory` | 管理六格、查询或修改并发布事件 | 喝水、治疗、撤离和文字显示 |
| `PlayerHydration` | 饮水值、自然下降和恢复 | 决定使用哪种物品 |
| `PlayerItemUser` | 选择物品效果，成功后请求消耗 | 直接改槽位、生命或饮水字段 |
| `ExtractionZone` | 检查撤离条件并记录是否完成 | 遍历背包内部和拾取物品 |
| `ExtractionResultUI` | 读取结算数据、显示面板、暂停和重新加载本局 | 判断是否满足撤离条件 |
| `GameMessageUI` | 显示临时文字并在延迟后隐藏 | 判断提示内容和业务成功条件 |
| `InventoryUI` | 读取六格并分给六个 View | 修改背包和拼单格文字 |
| `InventorySlotView` | 显示收到的一格数据 | 主动寻找背包和协调其他格 |

## 5. 完整功能链

### 拾取物品

```text
E键
→ PlayerController请求PlayerInteractor
→ PlayerInteractor找到最近的IInteractable
→ 实际对象WorldItemPickup执行Interact
→ PlayerInventory.TryAdd选择堆叠或空格
→ InventorySlot修改item和quantity
→ TryAdd返回成功，世界物品隐藏
→ InventoryChanged通知InventoryUI
→ InventoryUI逐格交给InventorySlotView显示
```

### 使用药品

```text
数字键
→ PlayerController请求PlayerItemUser
→ TryGetItem取得该格ItemData
→ PlayerItemUser按ItemType请求Health.TryHeal
→ 治疗成功返回true
→ PlayerInventory.TryConsume减少数量
→ InventoryChanged通知
→ UI重新读取并显示
```

### 携带样本撤离

```text
E键
→ PlayerInteractor找到最近目标，实际为ExtractionZone
→ ExtractionZone规定“必须持有样本”
→ PlayerInventory.HasItem只查询并返回bool
→ false时ExtractionZone直接请求GameMessageUI.Show
→ MessageText激活，两秒后由协程停用
→ 成功后ExtractionZone把completed设为true
→ ExtractionZone直接请求ExtractionResultUI.ShowSuccess
→ UI读取生命和饮水、显示结算面板并暂停本局
→ RestartButton.OnClick调用RestartGame
→ 恢复timeScale并重新加载当前场景
```

### 射击与伤害

```text
鼠标左键
→ PlayerController请求PlayerShooter
→ PlayerShooter通过ProjectilePool取得并激活子弹
→ Projectile自行移动和检测命中
→ 命中对象的Health.TakeDamage修改生命
→ 命中或寿命结束后归还对象池
```

## 6. 方法调用、返回值和事件

- 直接方法调用：当前流程明确请求某个对象执行操作。
- 返回值：被调用方法报告本次请求成功或失败，不是再次检测。
- 事件：数据修改完成后通知额外监听者，不代替直接方法调用。
- 当前撤离只有一个结算 UI，使用直接引用；加入音效、存档等多个监听者后再考虑撤离事件。

```text
PlayerItemUser调用TryConsume
→ TryConsume实际修改数据并返回bool
→ 修改成功后InventoryChanged.Invoke
→ 已订阅的InventoryUI.Refresh被调用
```

## 7. 面试复述模板

```text
我先按职责拆分输入、功能规则、数据和显示。
输入层只发请求，具体功能脚本判断行为能否成功；数据由所属组件用private字段保管，
外部只能调用public方法查询或修改，校验和状态变化集中在数据拥有者内部。
修改成功后通过事件通知UI，UI再从只读入口拉取最新数据。
不同交互对象用IInteractable统一调用，共享且稳定的物品配置用ScriptableObject资产复用。
```
