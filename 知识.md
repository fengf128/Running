# 知识

> 只记录可复用原理；具体操作和运行结果放在 `操作与作用.md`。

## 1. 项目与 Unity 基础

### 范围控制

- MVP：Minimum Viable Product，最小可行产品；先完成可完整体验的一局。
- 俯视角可减少镜头、动画和美术成本。
- 小地图、少系统、完整闭环比堆功能更适合作品验证。

### 版本与渲染管线

- LTS：Long Term Support，长期支持版本；重稳定，适合正式制作。
- 招聘更看 C#、Gameplay、调试和性能，不只看 Unity 版本。
- Built-in：旧素材兼容较好。
- URP：Universal Render Pipeline，通用渲染管线；轻量、跨平台。
- HDRP：High Definition Render Pipeline，高清渲染管线；画质和制作成本更高。
- 紫色材质通常表示 Shader 与当前渲染管线不兼容。
- Universal 3D 是空白 URP 模板；Sample 模板包含官方示例。

### 场景与布局

- `.unity` 保存场景对象、组件和序列化配置。
- 场景名称只表示用途，不改变功能。
- Unity 布局保存在 AppData，不属于项目资源。
- 布局警告通常不影响编译；应先清空 Console，再确认是否重现。

### Git

- 提交：Assets、Packages、ProjectSettings 和 `.meta`。
- 忽略：Library、Temp、Logs、UserSettings。
- `.meta` 保存 GUID（Globally Unique Identifier，全局唯一标识符），必须与资源一起提交。
- `init` 创建仓库；`add` 加入暂存区；`commit` 生成本地快照；`push` 上传远程。

## 2. C# 与 Unity 编程基础

### 类、实例与引用

- 类定义数据和行为；实例是运行时的具体对象。
- `new` 创建实例，变量保存该实例的引用。
- 引用可理解为受管理的“指向”，不是能做地址运算的裸指针。
- 未引用对象时，引用字段默认是 `null`。
- `this` 表示当前正在执行代码的实例，含义取决于所在类。
  - Pool 中的 `this` 是当前对象池。
  - Projectile 中的 `this` 是当前子弹。

### 声明类型与实际类型

- 声明类型决定编译时允许访问哪些成员；实际类型表示运行时真正创建的对象。
- 实际类型继承或实现声明类型时，同一对象引用可赋给更抽象的变量，不会创建或复制对象。
- `InventorySlot[]` 实现 `IReadOnlyList<InventorySlot>`，所以数组可通过只读列表接口被读取。
- 通过接口暴露数据可限制调用方能力并降低耦合；若调用方确实需要数组或 List 专有操作，直接使用具体类型更清楚。

### GameObject 与 Component

- GameObject 是场景对象；脚本、Transform 和 CharacterController 都是 Component。
- `GetComponent<T>()` 查找同一 GameObject 上的组件，不按对象名称搜索。
- `GetComponentInParent<T>()` 从当前对象向父级查找组件。
- `[RequireComponent]` 声明脚本依赖的组件。
- `[SerializeField] private` 保持字段封装，同时允许 Inspector 配置。

### 按能力组合对象

- 调用方优先查找对象具备的能力，而不是依赖对象名称或 Tag。
- Projectile 查找 `Health`：任何挂有 Health 的有效目标都可受伤。
- PlayerInteractor 查找 `IInteractable`：任何实现该接口的组件都可交互。
- Health 是具体组件；IInteractable 是由组件实现的共同规则，二者形式不同但都用于表达能力。
- 新增敌人、箱子或门时主要添加对应能力，原调用方通常不必增加具体类型判断。
- Tag 适合分类和筛选，但本身不提供扣血、交互等行为。

### 方法与控制流

- 方法封装一段职责明确的逻辑。
- 参数是调用方传入、只在本次方法调用中使用的局部变量。
- `对象.方法()` 中，点号左边先选定接收调用的具体实例；实例方法只处理该实例自己的字段。
- 被调用实例不必知道自己位于数组哪个下标；需要下标时由正在遍历数组的调用方保存和使用。
- `if` 判断条件；`return` 立即结束方法；`continue` 跳过本轮循环。
- `!` 对布尔值取反；`||` 表示任一条件成立。
- `out` 允许方法通过参数返回额外结果。
- `?.` 表示左侧不为 `null` 时才调用右侧成员。

### 属性与命名

- `CurrentHealth { get; private set; }`：外部可读，只有本类可改。
- `IsDead => CurrentHealth <= 0f`：表达式体只读属性。
- `=>` 此处等价于 `get { return CurrentHealth <= 0f; }`。
- `TakeDamage(float amount)` 中，`amount` 是本次传入的伤害量。
- 类、方法、属性使用大驼峰，如 `Health`、`TakeDamage`、`CurrentHealth`。
- 字段、参数、局部变量使用小驼峰，如 `maxHealth`、`amount`、`targetHealth`。
- `$"{value}"` 是字符串插值。

### 集合与内存

- `Queue<T>` 是先进先出泛型队列。
- `Enqueue` 从队尾加入；`Dequeue` 从队首取出；`Count` 是当前数量。
- `readonly` 字段不能重新指向其他对象，但数组内容仍可修改。
- GC：Garbage Collection，垃圾回收；负责回收不再可达的托管对象。
- GC 工作可能增加 CPU（Central Processing Unit，中央处理器）耗时并造成帧时间尖峰。

### 委托与事件

- `Action` 是可保存“无参数、无返回值方法”的委托类型。
- `InventoryChanged` 是开发者定义的事件名称；`event`、`Action` 和 `Invoke` 提供事件机制。
- `+= Refresh` 把目标对象和方法引用加入调用列表；`Refresh()` 才是立即调用。
- 事件不会自动观察字段；只有代码执行 `InventoryChanged?.Invoke()` 时才通知订阅者。
- `Invoke` 同步调用当前订阅方法；事件不保存历史，未订阅时发生的通知不会补发。
- `OnEnable` 订阅、`OnDisable` 取消订阅，可避免停用对象继续响应和重复订阅。
- 一个固定调用者可直接调用方法；一对多通知或希望发布者不认识接收者时适合事件；每帧轮询只适合确实每帧都需要检查的状态。

### Unity 生命周期

- `Awake`：初始化字段、缓存组件和创建运行时对象。
- `OnEnable/OnDisable`：对象启用或停用时执行。
- `Update`：每渲染帧执行，适合输入和一般逻辑。
- `FixedUpdate`：按固定时间步执行，适合物理移动。
- `LateUpdate`：Update 后执行，适合相机跟随。
- `OnTriggerEnter`：Trigger 发生重叠时执行。
- `OnDestroy`：对象销毁前释放资源。

## 3. Input System

### 结构

- 旧版输入常用 `Input.GetKey`、`Input.GetAxis`。
- 新版使用 InputAction，把玩家意图与具体设备按键解耦。
- Action Map 管理一组 Action；Action 表示玩家意图；Binding 指定实际按键。
- Action Type 决定读取方式，如 Button、Value。
- Control Type 决定返回格式，如 Vector2。
- Composite 把多个 Binding 合成一个值。
- 2D Vector 把上下左右合成 `Vector2`；Digital Normalized 避免斜向更快。

### 当前配置

- Move：`Value + Vector2`，WASD 的 2D Vector。
- Aim：`Value + Vector2`，Mouse Position。
- Fire：`Button`，Mouse Left Button。
- Interact：`Button`，Keyboard E。
- UseSlot1～UseSlot6：`Button`，Keyboard 1～6。

### 运行流程

1. `new PlayerInputActions()` 创建输入实例。
2. `Gameplay.Enable()` 启用 Action Map。
3. `ReadValue<Vector2>()` 连续读取 Move 和 Aim 数值。
4. `WasPressedThisFrame()` 查询按钮是否在本帧刚按下。
5. `Gameplay.Disable()` 停止响应；`Dispose()` 释放输入对象。
- 持续用 `ReadValue<float>()` 判断按钮会在按住期间每帧成立；单次交互更适合 `WasPressedThisFrame()`。

### 编辑器与生成代码

- Actions 面板管理 Action 和 Binding。
- Action Properties 编辑 Action；Binding Properties 编辑 Binding。
- Generate C# Class 生成强类型访问代码，避免字符串查找。
- 自动生成的 `PlayerInputActions.cs` 不手动修改。
- 编辑器界面报错不等于 C# 编译失败；先检查调用栈和是否存在 `error CS`。

## 4. 玩家、相机与瞄准

### CharacterController

- CharacterController 直接移动并处理碰撞，不依赖 Rigidbody 力。
- 它不会自动处理重力，需要脚本维护竖直速度。
- Player 保留 CharacterController 作为主要碰撞组件，不再重复保留 CapsuleCollider。

### PlayerController 职责

- PlayerController 是输入与动作的调度者，不是所有玩家功能的实现者。
- 它直接处理移动和瞄准；检测 Fire、Interact 后分别调用 PlayerShooter、PlayerInteractor。
- Awake 缓存同一 Player 上的组件引用，避免每帧重复查找。
- RequireComponent 保证 Player 同时具有移动、射击和交互所需组件。

### 移动流程

1. 读取 `Vector2` 移动输入。
2. 映射为世界坐标 `Vector3(x, 0, z)`。
3. 乘 `moveSpeed` 得到水平速度。
4. 根据接地状态和重力更新竖直速度。
5. 合并速度并乘 `Time.deltaTime` 得到本帧位移。
6. `CharacterController.Move` 执行移动。

### 相机跟随

- `Transform target` 保存目标 Transform 引用。
- `Vector3 offset` 保存相机与目标的相对位置。
- LateUpdate 中使用 `target.position + offset` 更新位置。
- `LookAt` 让相机朝向目标；空引用检查可避免运行错误。

### 鼠标瞄准

1. 读取鼠标屏幕坐标。
2. `ScreenPointToRay` 从相机生成射线。
3. Plane.Raycast 求射线与地面的交点距离。
4. `Ray.GetPoint` 得到世界交点。
5. 交点减 Player 位置得到方向。
6. 清零 y，只保留水平朝向。
7. 排除近零方向后，将单位方向赋给 `transform.forward`。
- 位置相减得到方向；交点本身不是方向。
- `normalized` 返回长度为 1 的新向量。
- `sqrMagnitude` 用长度平方判断近零，避免开方。

### Plane.Raycast 与 Physics.Raycast

- Plane.Raycast 检测数学平面，不需要 Collider，适合当前平坦地图。
- Physics.Raycast 检测场景 Collider，可读取命中对象、交点和法线，适合有高度变化的地图。

## 5. 战斗系统

### 调用关系

```text
PlayerController → PlayerShooter → ProjectilePool → Projectile
RangedEnemy ─────────────────────→ ProjectilePool
Projectile → Health
Projectile → ProjectilePool.Release
```

### Health

- `CurrentHealth` 保存当前生命；`IsDead` 根据生命是否小于等于 0 计算。
- `TakeDamage(amount)` 验证伤害后扣血，`Mathf.Max` 保证生命不低于 0。
- `TryHeal(amount)` 在存活且未满血时治疗，`Mathf.Min` 保证不超过上限；返回值说明效果是否成功。
- 生命归零时停用 GameObject，其 Update 等生命周期方法也停止执行。

### 射击与冷却

- PlayerController 只读取 Fire；PlayerShooter 负责冷却和发射。
- FirePoint 提供子弹生成位置与旋转，并继承角色朝向。
- `Time.time < nextFireTime` 表示仍在冷却。
- 发射后用 `Time.time + fireCooldown` 计算下一次允许时间。

### 对象池

- 预热：开始时批量创建并停用对象。
- Spawn：从 Queue 取出并启用；Release：停用并归还队列。
- 作用：减少反复 Instantiate、Destroy 和 GC，令帧耗时更稳定。
- 代价：提前占用内存，且每次复用前必须重置状态。
- Queue 只保存未激活、可复用的子弹。
- Initial Size 是预热数量，不是容量上限。
- Queue 为空时会创建一颗并直接发射；回收后加入 Queue，池永久扩充一个。
- 扩容条件是“同时活跃对象耗尽库存”，不是累计射击次数超过 20。

### 子弹生命周期

1. Pool 创建子弹，调用 `Initialize(this)` 记录所属 Pool。
2. Spawn 调用 Launch，设置位置、旋转、发射者、寿命和业务状态。
3. `SetActive(true)` 让 Unity 调用 FixedUpdate；`isLaunched` 允许移动逻辑执行。
4. 每个物理帧用 `Rigidbody.MovePosition` 沿 `transform.forward` 移动。
5. 命中或超时后清理状态，调用所属 Pool 的 Release。
- Initialize 每颗子弹只调用一次；Launch 每次复用都调用。
- 激活本身不提供速度，真正移动来自 FixedUpdate 中的代码。

### 物理与伤害

- SphereCollider 检测命中；Kinematic Rigidbody 由脚本控制移动。
- Is Trigger 只报告重叠，不产生推挤和反弹。
- Continuous Speculative 用于减少高速穿透。
- 子弹通过 `GetComponentInParent<Health>()` 查找目标生命组件。
- 每发子弹保存发射者 `sourceHealth`；目标引用相同则忽略自身。
- 命中自身 Health 时忽略并继续飞行；命中其他 Collider 后，无论有无 Health 都回收。

## 6. 敌人

### ChaserEnemy

- Player 在检测范围外：待机。
- 在检测范围内、攻击距离外：朝目标直接移动。
- 进入停止距离：按冷却调用 Player Health。
- 当前不使用 NavMesh（Navigation Mesh，导航网格），不处理绕障寻路。

### RangedEnemy

- 固定站位，不追击 Player。
- Player 进入 12 米后持续转向，冷却结束时通过共享 Pool 发射。
- 当前只忽略发射者自己，敌人之间仍可能互相命中。

### 距离与目标

- `target.position - transform.position` 得到指向目标的向量。
- `sqrMagnitude` 返回 `x² + y² + z²`，即距离平方。
- 必须使用未归一化向量；归一化会丢失距离信息。
- 距离平方与范围平方都是标量比较，不是逐个坐标比较。
- `FindGameObjectWithTag` 在 Awake 查找一次 Player，避免每帧全场搜索。

## 7. 统一交互

### IInteractable

- interface 是共同规则，不是结构体，也不能单独 `new IInteractable()`。
- 接口中的 `Interact(GameObject interactor);` 只有调用形式，没有方法体；不能直接执行。
- 实现接口不是扩充原函数，而是用相同签名提供接口要求的具体方法体，如拾取或开门。
- `IInteractable closestInteractable` 是 PlayerInteractor 声明的接口引用变量，不是接口内部的变量。
- `GetComponentInParent<IInteractable>()` 返回实际组件引用；找到更近目标时由 `closestInteractable = interactable` 保存该引用。
- 接口引用指向的实际对象仍是 WorldItemPickup、Door 等；调用时执行该对象自己的 Interact 实现。
- `InteractionName` 描述被交互目标，如“近补给箱”。
- `interactor` 指向发起交互的对象，当前是 Player。
- 两者是独立信息：一个用于显示目标，一个用于访问发起者组件。
- 传 GameObject 后，物品可通过它取得 PlayerInventory、Health 等组件。

### 范围检测

- 按 E 时调用一次 `OverlapSphereNonAlloc` 球形重叠检测。
- Non-Allocating 表示不新建结果数组；固定复用 `Collider[16]`。
- 检测取得范围内 Collider，再过滤实现 `IInteractable` 的对象。
- `ClosestPoint` 取得 Collider 表面离 Player 最近的点。
- 比较距离平方后，只调用最近目标的 Interact。
- 范围按 Collider 是否与球体相交判断，不按对象中心判断。
- 当前是 360 度检测，不检查朝向和墙体；LayerMask 暂为 Everything。
- `~0` 表示 LayerMask 的所有位为 1，即检测全部 Layer。

### 调用流程

```text
Keyboard E
→ InputActions.Gameplay.Interact.WasPressedThisFrame()
→ PlayerController.UpdateInteract()
→ PlayerInteractor.TryInteract()
→ 最近 IInteractable.Interact(Player)
```

## 8. 物品与背包

### ItemData

- ScriptableObject 是独立数据资产，不需要挂在场景 GameObject 上。
- ScriptableObject 和 `[CreateAssetMenu]` 都属于 UnityEngine，不是额外安装的包。
- Unity 编译脚本后读取 `menuName`，把创建入口注册到 Assets 的右键菜单。
- 在任意 Assets 子文件夹中调用菜单，生成的 `.asset` 就保存在当前文件夹。
- 同一种物品的多个世界实例可引用同一个 ItemData，避免重复配置。
- `ItemData item` 保存完整 ItemData 资产的引用，不是只保存 ItemType 枚举。
- `ItemType` enum 限定 Water、Medical、Quest、Other 四种类型。
- `ItemType` 只是 ItemData 内的一个字段；名称、堆叠上限和效果值也是该资产的字段。
- ItemData 保存稳定 ID、显示名、堆叠上限和效果数值，不负责拾取行为。
- `[CreateAssetMenu]` 在 Assets 创建菜单中添加数据资产入口。

### InventorySlot

- `[System.Serializable]` 让普通 C# 类可被 Unity 序列化显示。
- InventorySlot 是 class，不是 struct；背包保存的是六个槽位对象引用。
- 一个槽位保存 ItemData 引用和数量，不复制整份物品数据。
- `item = itemToAdd` 让槽位引用传入的同一资产，不会复制资产字段；之后可通过 `item.DisplayName` 等属性读取数据。
- `item != itemToAdd` 比较资产引用；字段相同但属于两个不同 asset 的物品仍不会堆叠。
- `IsEmpty` 同时检查物品引用和数量。
- `TryStack` 只给相同 ItemData 且未满的槽位加一。
- `TryFill` 只把物品放入空槽，并把数量设为一。
- PlayerInventory 用 `slots[i]` 选中具体槽位实例，再调用它的 TryStack 或 TryFill；InventorySlot 本身不保存格子编号。
- `TryConsumeOne` 只修改当前槽位实例：数量减一；归零时清除 item 引用，使 IsEmpty 恢复为 true。

### PlayerInventory

- `new InventorySlot[6]` 创建六个引用位置；每个元素初始仍是 null。
- 循环 `new InventorySlot()` 后，六个槽位才各自拥有实例。
- `TryAdd` 先遍历已有堆叠，再遍历空槽，避免同类物品过早占新格。
- `IReadOnlyList<InventorySlot>` 允许外部读取槽位，不允许替换集合元素。
- 添加失败返回 false，世界物品因此不会消失。
- 第一次拾取：堆叠遍历失败，再填入索引 0 的空槽。
- 再次拾取同一 ItemData：索引 0 直接堆叠，不占新槽。
- `TryConsume(slotIndex)` 由下标选择槽位；成功修改后再次触发 InventoryChanged，使原有 UI 刷新链可复用。
- TryConsume 不是检测数据有没有改变，而是主动减少指定槽位；bool 只报告是否成功。

### 物品使用

- PlayerItemUser 协调物品效果，不保存背包数据，也不直接修改生命或饮水字段。
- `TryGetItem(index, out item)` 返回是否取得物品，并通过 out 参数交出 ItemData 引用。
- Water 调用 PlayerHydration.TryRestore；Medical 调用 Health.TryHeal；其他类型不能直接使用。
- 先确认效果成功，再调用 TryConsume，避免满血或满饮水值时浪费物品。
- TryConsume 成功后由 `InventoryChanged?.Invoke()` 通知 UI；检测、修改、通知是三件不同的事。
- PlayerHydration 用 `drainPerSecond * Time.deltaTime` 表示每秒消耗，避免帧率影响速度。

### 新增物品的两种情况

- 新物品沿用已有 Water 或 Medical 行为时，只需新建 ItemData、填写数值并让世界拾取物引用它。
- 多个世界拾取物应引用同一个 ItemData 资产；不同 ItemData 资产即使字段相同也不会互相堆叠。
- 新物品需要全新行为时，如加速或投掷，需要扩展 ItemType、效果接收组件和 PlayerItemUser 分支。
- 同类物品只改配置属于数据驱动；少量稳定类型用 switch 足够，类型持续增加时再考虑独立效果策略。

### WorldItemPickup

- 当前拾取不依赖 Tag；WorldItemPickup 用 `itemData` 说明这个世界物体代表哪种物品。
- 它实现 IInteractable，把 ItemData 名称作为 InteractionName，并提供拾取版本的 Interact 方法体。
- `interactor.GetComponent<PlayerInventory>()` 从发起者取得背包。
- 调用时 `interactor` 是 Player；WorldItemPickup 的 `gameObject` 是被拾取的世界物品。
- `TryAdd` 成功后才 `SetActive(false)`，保证满包时物品仍留在世界。
- `condition ? a : b` 是三元运算符：条件成立返回 a，否则返回 b。

## 9. 背包 UI

### 组件与属性

- Canvas 提供屏幕 UI 根节点；Canvas Scaler 用参考分辨率和 Match 适配屏幕。
- RectTransform 使用锚点、轴心、位置和尺寸控制 UI 布局。
- Vertical Layout Group 自动排列子物体；Padding 控制内边距，Spacing 控制间隔，Control Child Size 接管尺寸，Force Expand 填充剩余空间。
- Layout Element 提供首选尺寸；TextMeshProUGUI 负责文字显示。
- Input System UI Input Module 对接新版输入；Standalone Input Module 使用旧输入接口。

### 数据与显示

- `InventorySlotView[]` 保存多个已有组件引用；Inspector 的 Size 和 Element 由 Unity 序列化，不会创建组件实例。
- `InventoryUI` 引用数据源和六个 View；每个 `InventorySlotView` 引用自己的 TMP_Text。
- `inventory.Slots[i]` 是第 i 格背包数据；`slotViews[i]` 是负责显示第 i 格的 UI 组件，两者不是同一个对象。
- InventoryUI 的 for 循环每轮只把一格数据传给同下标的一个 View；六轮结束后六格全部刷新。
- `InventoryUI.Refresh` 协调整个列表，`InventorySlotView.Refresh` 只显示一格，避免整体类同时负责集合与文字格式。
- 六格背包完整刷新简单且成本低；大型列表可让事件携带变化索引，只刷新对应项。

### 事件驱动与轮询

- 数据改变时触发事件，UI按需刷新；相比在 `Update` 每帧轮询，减少无变化时的重复工作。
- 事件适合背包、生命、任务和设置变化；只有一个简单标签时直接调用更新方法更直观。

### 字体

- 原始 `.otf/.ttf` 提供字形；TMP SDF 资产提供字符表、图集和材质。
- Dynamic 字体按需加入字符，开发方便但源字体进入 Build；Static 子集运行开销和体积更可控，但新增字符需重新生成。

## 10. 完成与证据

- 安装包、创建脚本不等于功能完成。
- 功能完成需要场景配置、运行验证和可复现结果。
- 性能优化需要 Profiler 数据，不能只凭感觉。
- 尚未实现或未验证的功能不能写入简历。
