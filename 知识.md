# 知识

> 只记可复用概念，一点一条。

## 范围

- MVP：能完整玩一局的最小版本。
- 俯视角：减少动画、镜头和美术成本。
- 小范围：更容易完成、验证和讲清。

## Unity 版本

- LTS：重稳定，适合正式制作。
- 新版本：不等于作品质量更高。
- 招聘：更看 C#、Gameplay、调试和性能。

## 渲染管线

- Built-in：旧素材兼容好。
- URP：轻量、跨平台。
- HDRP：画质高，成本高。
- 紫色材质：通常是 Shader 不兼容。
- 素材导入：先看 Unity 版本和管线兼容项。

## 模板

- Universal 3D：空白 URP 模板。
- Universal 3D sample：带官方示例。
- 作品用空白模板：代码归属更清楚。

## 场景

- `.unity`：保存场景中的对象和组件。
- 场景名只表示用途，不改变功能。
- 另存场景：保留原文件并生成新文件。

## Unity 布局

- 布局保存在 AppData，不属于项目。
- 布局警告通常不影响编译和打包。
- 排查日志要先 Clear，再看是否重现。

## Git

- 提交：Assets、Packages、ProjectSettings、`.meta`。
- 忽略：Library、Temp、Logs、UserSettings。
- `.meta`：保存资源 GUID，必须提交。
- `init`：创建仓库。
- `add`：加入暂存区。
- `commit`：生成本地快照。
- `push`：上传远程仓库。

## C#

### 构建配置

- Debug：方便断点，开发时使用。
- Release：优化更多，调试较弱。
- 编辑器模式不等于 Player 构建配置。

### 对象与引用

- 类：定义数据和行为的类型。
- 实例：类在运行时创建出的具体对象。
- 实例化：用 `new` 创建实例。
- 引用变量：保存对象引用，可理解为受管理的“指向”。
- 引用不是裸指针：不能做地址运算，由 GC 管理。
- 未引用对象时，字段默认值是 `null`。

### 方法与控制流

- 方法：封装一段职责明确的逻辑。
- `if`：满足条件时执行代码块。
- `!`：对布尔值取反。
- `return`：立即结束当前方法。
- `out`：让方法通过参数返回额外结果。

## Input System

### 输入方案

- 旧版：`Input.GetKey`、`Input.GetAxis`。
- 新版：`InputAction`、`PlayerInput`。
- Active Input Handling：
  - `New`：只启用新版。
  - `Both`：新旧版同时启用。

### 资源层级

- Action Map：管理一组 Action。
- Action：表示玩家意图。
  - Action Type：读取方式，如 `Button`、`Value`。
  - Control Type：返回格式，如 `Button`、`Vector2`。
  - Button Action：按钮语义已确定，Control Type 可为空。
  - Interactions：按下、长按等触发规则。
- Binding：指定实际输入设备或按键。
  - Composite：把多个 Binding 合成一个值。
  - 2D Vector：把上下左右合成 `Vector2`。
  - Digital Normalized：避免斜向移动更快。

### 编辑器界面

- Actions：管理当前 Action Map 的 Action 和 Binding。
- Action Properties：编辑选中 Action。
- Binding Properties：编辑选中 Binding。
- Generate C# Class：生成强类型访问代码。
  - 优点：避免用字符串查找 Action。
  - 注意：生成文件不手动修改。

### 报错判断

- GUI 报错不等于 C# 编译失败。
  - 先看调用栈来源。
  - 再查是否存在 `error CS`。
- `InputControlPathEditor` 报错可能只是编辑器重绘异常。

### 已用配置

- `Move`
  - 类型：`Value + Vector2`。
  - 绑定：WASD 的 2D Vector Composite。
  - 输出：二维移动方向。
- `Aim`
  - 类型：`Value + Vector2`。
  - 绑定：`Mouse/Position`。
  - 输出：鼠标屏幕坐标。
- `Fire`
  - 类型：`Button`。
  - 绑定：`Mouse/Left Button`。
  - 输出：开火按下状态。
- `Interact`
  - 类型：`Button`。
  - 绑定：`Keyboard/E`。
  - 输出：交互按下状态。

## 玩家控制

### CharacterController

- 用途：直接移动并处理碰撞。
- 特点：不依赖 Rigidbody 力。
- 注意：不会自动处理重力。

### 碰撞组件

- Player 只保留一个主要碰撞组件。
- 添加 CharacterController 后删除 CapsuleCollider。

### 组件代码

- `[RequireComponent]`：声明脚本依赖的组件。
- `[SerializeField] private`：保持封装并允许 Inspector 配置。

### GameObject 与 Component

- Player 是 GameObject，脚本和 CharacterController 是其 Component。
- `GetComponent<T>()`：查找同一 GameObject 上的 `T` 组件。
- `GetComponent<T>()` 不按对象名称搜索。

### Unity 生命周期

- `Awake`：缓存组件并创建输入对象。
- `OnEnable/OnDisable`：启用和停用输入。
- `Update`：每帧读取输入并移动。
- `LateUpdate`：在 Update 后执行，适合相机跟随。
- `OnDestroy`：释放输入对象。

### 移动计算

1. `ReadValue<Vector2>()` 读取移动输入。
2. `Vector2(x, y)` 映射到世界的 `Vector3(x, 0, z)`。
3. 水平速度乘 `moveSpeed`。
4. 竖直速度根据接地和重力更新。
5. 水平与竖直速度合并。
6. 速度乘 `Time.deltaTime` 得到本帧位移。
7. `CharacterController.Move` 执行位移。

### 相机跟随

- `Transform target`：保存目标 Transform 的引用。
- `Vector3 offset`：保存相机与目标的相对位置。
- `target == null`：避免访问空引用。
- `transform`：当前脚本所在 GameObject 的 Transform。
- `LookAt`：让相机朝向目标位置。

### 鼠标瞄准

#### 通用流程

- 屏幕坐标不能直接与世界坐标计算方向。
- 交点是世界位置，本身不表示方向。
- 两个位置相减得到方向向量。
- 地面朝向由方向向量的 x、z 共同表示。
- `Ray.GetPoint(d)`：计算 `起点 + 方向 × 距离`。
- `sqrMagnitude`：计算向量长度平方，避免开方。
- 近零方向不能表示稳定朝向，需要提前返回。
- `normalized`：返回长度为 1 的新向量，不修改原变量。
1. Aim 读取鼠标屏幕坐标。
2. `ScreenPointToRay` 生成相机射线。
3. Raycast 求射线命中的世界坐标。
4. 交点减玩家位置得到瞄准方向。
5. 清零 y，限制角色只绕竖直轴旋转。
6. `normalized` 得到单位方向并赋给 `transform.forward`。
- `sqrMagnitude`：无需开方即可判断方向是否接近零。

#### Plane.Raycast

- 检测数学平面，不需要 Collider。
- 返回射线距离，再用 `Ray.GetPoint` 求交点。
- 适合当前平坦地图。

#### Physics.Raycast

- 检测场景 Collider，需要地面碰撞体。
- 返回 `RaycastHit`，可读取交点、法线和命中对象。
- 适合有高度变化或需要识别对象的场景。

## 证据

- 安装包不等于完成功能。
- 功能要有代码和运行验证。
- 优化要有 Profiler 数据。
- 未实现内容不能写进简历。
