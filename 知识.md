# 知识

> 只记录可迁移到其他Unity项目的概念和原理；本项目的操作、职责和调用链分别见`操作与作用.md`、`思路.md`。

## 1. 项目基础

### 范围与版本

- MVP（Minimum Viable Product，最小可行产品）：先做出一局可完整体验、可验证的最小版本。
- 小地图、少系统和完整闭环，比堆叠未完成的功能更适合作品展示。
- LTS（Long Term Support，长期支持）强调稳定，适合正式制作。
- Built-in兼容较多旧素材；URP轻量且跨平台；HDRP画质和制作成本更高。
- 紫色材质通常表示Shader与当前渲染管线不兼容。

### 场景与资源

- `.unity`保存场景对象、组件和序列化配置；场景名称本身不改变功能。
- `.meta`保存资源GUID，必须与资源一起移动和提交。
- Assets、Packages、ProjectSettings应提交；Library、Temp、Logs、UserSettings通常忽略。
- Git中`init`创建仓库，`add`加入暂存区，`commit`生成本地快照，`push`上传远程。

## 2. C#、GameObject与引用

### 类、实例和引用

- 类定义一类对象的数据和行为；实例保存具体运行状态。
- `new`创建实例；引用变量保存对实例的受管理指向，不是可进行地址运算的裸指针。
- 多个引用变量可以指向同一实例；通过任一引用修改后，其他引用读到的是同一份状态。
- 引用未指向对象时为`null`；`this`表示当前正在执行代码的实例。
- 数组本身也是对象；`new T[6]`创建一个数组实例和六个元素位置。若`T`是class，元素初始为`null`，不会自动创建六个`T`实例。

### GameObject与Component

- GameObject是场景对象；Transform、Collider、Canvas和MonoBehaviour脚本都是Component。
- `对象.GetComponent<T>()`只在点号左边那个GameObject上查找已有组件并返回引用，不创建新组件，也不搜索整个场景。
- 脚本内省略点号的`GetComponent<T>()`等价于`gameObject.GetComponent<T>()`。
- `GetComponentInParent<T>()`从当前对象向父级查找。
- `[RequireComponent]`声明同一GameObject必须具有的依赖组件。

### Inspector引用

- `[SerializeField] private T field`保持代码私有，同时允许Unity序列化和Inspector赋值。
- 字段类型决定可以保存什么组件；拖入Hierarchy对象时，Unity取得该对象上匹配类型的组件实例。
- 拖入的是场景组件实例，不是Project窗口中的`.cs`代码文件。
- 固定且跨对象的场景连接适合Inspector绑定；已获得某个运行时对象并要取它自身能力时适合GetComponent。
- 例如结算UI是固定对象，直接保存`ExtractionResultUI`引用；撤离者由本次调用决定，从传入Player取得Health。

### 声明类型与实际类型

- 声明类型决定当前代码允许访问哪些成员；实际类型表示运行时对象究竟是什么。
- 实际类型实现接口或继承声明类型时，同一对象引用可以赋给更抽象的变量，不会复制或改变对象。
- `IInteractable target`可以指向实际的WorldItemPickup或ExtractionZone；调用时执行实际对象的方法体。
- `InventorySlot[]`实现`IReadOnlyList<InventorySlot>`，因此UI可用只读接口读取同一个数组。

### 方法、参数与属性

- `对象.方法(实参)`中，点号左边决定谁执行；括号内容是本次调用传入的信息。
- 方法声明中的参数是形参；调用时传入的具体值是实参。
- `out`允许方法除返回值外再交出数据；`?.`表示左侧非null时才调用。
- `if`判断条件，`return`结束方法，`continue`跳过当前循环。
- `CurrentHealth { get; private set; }`表示外部可读、只有本类可写。
- `IsDead => CurrentHealth <= 0f`是表达式体只读属性。
- `$"{value}"`是字符串插值；类/方法/属性用大驼峰，字段/参数/局部变量用小驼峰。

### 集合与内存

- `Queue<T>`先进先出；`Enqueue`加入队尾，`Dequeue`取出队首。
- `readonly`字段不能重新指向其他对象，但引用对象内部仍可能变化。
- GC（Garbage Collection，垃圾回收）负责回收不可达托管对象；频繁分配可能造成CPU耗时和帧尖峰。

### Unity生命周期

- `Awake`：初始化和缓存组件。
- `OnEnable/OnDisable`：启用或停用时订阅、取消订阅或管理输入。
- `Update`：每渲染帧执行，适合输入和一般逻辑。
- `FixedUpdate`：固定时间步，适合Rigidbody物理移动。
- `LateUpdate`：Update之后执行，适合相机跟随。
- `OnTriggerEnter`：Trigger重叠时执行。
- `OnDestroy`：销毁前清理资源或恢复全局状态。

## 3. 设计工具

### 封装

- 数据由所属对象保管，外部通过public方法查询或请求修改；封装不要求使用`interface`。
- public方法表示允许的操作，真正规则是方法体中的检查和状态变化。
- 例如PlayerInventory保管private槽位，公开TryAdd、TryConsume和HasItem；外部不直接遍历和替换数组。
- 校验、修改、归零清理和通知集中在数据拥有者，可避免多个脚本重复规则或制造无效状态。

### 接口与按能力组合

- C# `interface`规定共同成员，不保存具体状态，也不能直接`new`接口实例。
- 实现类必须提供方法体，并可拥有接口没有规定的额外字段和方法。
- 同一调用者要统一处理多种具体对象时适合接口；只有一个固定类型时直接调用更简单。
- Projectile查找Health、PlayerInteractor查找IInteractable，都是按对象能力组合，不依赖名称。
- Tag适合分类和筛选，但本身不提供受伤、交互等行为。

### 直接调用、返回值和事件

- 直接调用适合明确的一对一请求；调用方知道目标并需要立即执行。
- `bool`返回值报告本次调用成功或失败，不是再次检测数据。
- 事件适合一个变化通知零个或多个独立监听者，发布者不需要认识具体接收者。
- `Action`可保存无参数、无返回值的方法引用；`+=`订阅，`-=`取消，`?.Invoke()`同步通知。
- 事件不会自动观察字段，只有显式Invoke才发生；不保存历史，未订阅者不会补收旧通知。
- `OnEnable`订阅、`OnDisable`取消，可避免停用对象继续响应或重复订阅。

### ScriptableObject与数据驱动

- ScriptableObject可保存为独立`.asset`，适合共享且相对稳定的配置。
- `[CreateAssetMenu]`只是在Assets创建菜单中注册入口，不是额外包。
- 场景物体和运行状态保存资产引用，不重复复制名称、类型和数值。
- 物品名称、类型、堆叠上限和效果值适合ItemData；当前数量、当前生命和是否撤离不适合共享资产。
- 数据驱动表示同一行为通过更换配置产生不同结果；同类水或药只需新资产，全新效果才扩展代码。

## 4. 输入、移动与空间计算

### Input System

- Action Map管理一组Action；Action表示玩家意图；Binding指定设备按键。
- Action Type决定行为，如Button、Value、PassThrough；Control Type决定返回数据，如Vector2。
- 2D Vector Composite把WASD合成Vector2；Digital Normalized避免斜向速度更快。
- `ReadValue<T>()`读取连续值；`WasPressedThisFrame()`适合只触发一次的按键。
- Generate C# Class提供强类型访问；生成文件不手动修改。

### CharacterController与时间

- CharacterController直接处理移动碰撞，不依赖Rigidbody受力，也不会自动施加重力。
- 水平速度和竖直速度合并后乘`Time.deltaTime`，使移动速度不依赖渲染帧率。
- 相机在LateUpdate跟随，可减少目标先移动、相机后更新造成的视觉抖动。

### 鼠标瞄准与向量

- `ScreenPointToRay`从相机和鼠标屏幕位置生成世界射线。
- Plane.Raycast适合无Collider的数学平面；Physics.Raycast命中场景Collider并返回对象、交点和法线。
- 射线交点减Player位置得到方向；清零y只保留水平朝向。
- `normalized`保留方向并把长度变为1；归一化后会丢失原距离。
- `sqrMagnitude=x²+y²+z²`是距离平方；与范围平方比较可避免开方。

### 物理组件

- Trigger只报告重叠，不产生推挤；触发回调至少需要合适的Collider/Rigidbody组合。
- Kinematic Rigidbody由代码控制，不接受普通物理力推动。
- 高速物体可使用Continuous Speculative减少穿透。

## 5. 战斗与对象池

### Health与冷却

- Health拥有当前生命，TakeDamage和TryHeal是受控修改入口。
- `Mathf.Max`避免生命低于0，`Mathf.Min`避免治疗超过上限。
- 冷却常用`Time.time < nextTime`判断；成功执行后更新下一次允许时间。

### 对象池

- 预热：开始时创建并停用一批对象。
- Spawn：从可用Queue取出、重置并激活；Release：清理、停用并归还。
- 作用是减少频繁Instantiate、Destroy和GC，使帧时间更稳定。
- 代价是提前占用内存，并要求每次复用都正确重置状态。
- Initial Size只是预热数量；Queue为空时可新建，回收后池永久扩容。
- 扩容取决于同时活跃数量耗尽库存，不取决于累计发射次数。

### 子弹生命周期

- Initialize通常只在创建时记录所属Pool；Launch在每次复用时设置位置、方向、伤害来源和寿命。
- `SetActive(true)`只启用对象；真正位移来自FixedUpdate中的Rigidbody.MovePosition。
- 命中或超时后归还Pool；发射者引用用于忽略自身Health。

### 敌人

- 简单敌人可按距离切换待机、追击/转向和攻击状态。
- `target.position-transform.position`得到目标方向；未归一化向量的sqrMagnitude保留距离信息。
- 场景只在初始化时查找Player，避免每帧全场搜索。
- 无NavMesh的直接移动不处理绕障，适合当前小型开阔测试场。

## 6. 交互、物品与背包

### 统一交互

- IInteractable统一交互名称和Interact调用形式；WorldItemPickup和ExtractionZone提供各自实现。
- `OverlapSphereNonAlloc`复用预先创建的Collider数组，避免每次检测分配新数组。
- 先过滤具有IInteractable能力的对象，再比较Collider到Player的距离平方，只调用最近目标。
- `InteractionName`描述被交互目标；`interactor`是发起交互的GameObject，二者不是同一方。

### ItemData与槽位

- ItemData引用整份物品资产；ItemType只是其中一个枚举字段。
- InventorySlot是class实例，保存一个ItemData引用和独立quantity，不复制资产字段。
- `item==itemToAdd`比较资产引用；两个字段相同但资产不同的物品不会堆叠。
- TryStack只处理同资产且未满的槽位；TryFill只处理空槽；TryConsumeOne归零时清除item引用。

### PlayerInventory

- 固定六格适合数组；PlayerInventory通过下标选择具体InventorySlot实例。
- TryAdd先遍历堆叠，再遍历空槽，避免相同物品过早占新格。
- 添加失败返回false，世界拾取物因此保留。
- TryConsume主动修改指定槽位；HasItem只查询，不修改也不触发变化事件。
- `IReadOnlyList<InventorySlot>`允许UI读取数量和索引，但不替换集合元素。

### 物品效果

- PlayerItemUser协调“使用哪种效果”，不保存背包、生命或饮水数据。
- `TryGetItem(index,out item)`先取得ItemData；效果成功后才TryConsume，避免满值时浪费物品。
- 持续数值变化使用“每秒值×deltaTime”，避免不同帧率产生不同速度。

## 7. UI与游戏流程

### uGUI布局与文字

- Canvas是屏幕UI根节点；Canvas Scaler用参考分辨率和Match适配不同屏幕。
- RectTransform通过锚点、轴心、位置和尺寸控制布局。
- Vertical Layout Group排列子物体；Layout Element提供首选尺寸。
- TMP原始`.otf/.ttf`提供字形；SDF资产保存字符表、图集和材质。
- Dynamic SDF开发方便但需携带源字体；Static子集体积可控但新增字符需重新生成。

### 列表UI与事件刷新

- InventoryUI负责读取集合和协调六格；InventorySlotView只负责格式化一格文字。
- `slots[i]`是第i格数据，`slotViews[i]`是第i格显示组件，二者不是同一个对象。
- 小型固定列表可在变化时整体刷新；大型列表可让事件携带索引，只刷新变化项。
- 事件驱动避免Update每帧重复读取没有变化的数据。

### UI输入与Button

- uGUI点击需要EventSystem、GraphicRaycaster和Input System UI Input Module。
- Point提供鼠标位置，Left Click提供主按钮；任一未绑定都无法形成鼠标点击。
- Gameplay和UI可以使用不同Action资产；当前玩家读Gameplay，EventSystem读DefaultInputActions的UI Map。
- Button的On Click保存目标组件和public方法引用，点击后由EventSystem调用。

### 协程与临时提示

- `IEnumerator`配合`yield return`可暂停一段方法，并在等待结束后继续。
- `StartCoroutine`返回Coroutine引用；保存后可用StopCoroutine终止旧流程。
- `WaitForSecondsRealtime`使用真实时间，不受`Time.timeScale`影响。
- 重复提示时先停止旧协程再重新开始，使隐藏时间从最后一次请求计算。
- 停用MessageText不会停用挂在Canvas上的GameMessageUI，因此仍可接收下一次Show。

### 暂停与场景重载

- `Time.timeScale=0`使依赖缩放时间的移动、物理和数值变化停止，UI仍可处理输入。
- 重新加载前恢复timeScale，避免新场景继续暂停。
- `SceneManager.GetActiveScene()`取得当前场景；`LoadScene(buildIndex)`要求场景已加入Scenes In Build。

## 8. JSON存档

- `[Serializable]`让普通C#类的字段可被Unity序列化；`JsonUtility`不负责保存运行时组件和它们之间的引用关系。
- 存档数据类只装数字、字符串、数组等必要字段，也叫DTO（Data Transfer Object，数据传输对象）。
- 快照是在某一时刻把值复制到新的DTO实例；之后运行状态变化，不会自动修改已有快照。
- JSON保存稳定且唯一的`itemId`和数量；读取时再用ID找回已有`ItemData`资产引用。
- 保存是“运行对象→纯数据快照→JSON字符串→文件”，读取是反向解析后请求对象恢复。
- 修改一组状态前先完成整体校验；校验失败应在循环修改前返回，避免只恢复一半。
- `if`和`bool`处理可预期的失败；`try/catch`处理文件权限、内容损坏等执行异常，返回`false`不会进入`catch`。
- 恢复应调用数据拥有者的public入口，继续执行范围检查、堆叠上限和事件通知，而不是绕过private字段。
- `Path.Combine`组合路径，避免手写斜杠；运行时文件不应放进Assets，防止资源导入和Git污染。

## 9. Profiler与性能证据

### 基础指标

- 帧时间是完成一帧所需时间；60 FPS预算约16.67 ms，不能只看瞬时FPS。
- CPU负责脚本、物理和渲染准备，GPU负责实际绘制；二者耗时需分开判断。
- Timeline按时间展示线程工作；Hierarchy按调用关系统计当前帧的耗时、次数和分配。
- `Total`包含子调用，`Self`只算方法自身，`Calls`是当前帧调用次数。
- `Total/Time ms`是同一总耗时的百分比/毫秒表示；`Self/Self ms`是排除子调用后的百分比/毫秒表示。
- EditorLoop属于Unity编辑器；PlayerLoop包含游戏运行时的引擎系统和用户脚本，不只代表自己写的代码。
- PlayerLoop是顶层运行循环标记；它的Total只包含自身及其子调用，不包含同级的EditorLoop等其他分支。
- 方法没有可见的子采样时，`Time ms`与`Self ms`可以相等；有子调用被单独记录时，通常是Total大于Self。
- `GC Alloc`是当前帧新产生的托管内存分配；对象失去引用后才成为垃圾，不是分配时就一定是垃圾。
- 临时分配是为短期工作新建字符串、数组或对象；它先占内存，不再被引用后等待GC回收。
- 尖峰是某帧耗时突然升高；应点选该帧确认是PlayerLoop、EditorLoop、GC还是其他工作，不能只看曲线高度。
- 渲染帧和物理步不是一一对应；物理按固定间隔运行，因此部分帧的Physics和`FixedUpdate`可能为0。
- GC Used是已占用托管内存；GC Alloc、GC Used和内存泄漏是不同概念。
- `WaitForSignal`等等待标记不等于正在计算；应继续判断它在等谁。

### 测量边界

- Editor数据包含EditorLoop、Profiler和资源窗口开销，适合学习与前后对比，不等于Player数据。
- Memory Simple显示选中帧的内存状态，不是所有帧相加；相邻帧变化小，所以数值常看起来不变。
- `In use/Reserved`是当前使用量/Unity预留池大小；Reserved不是固定上限，需要时Unity可继续向系统申请。
- 单个普通帧只能说明该帧；还需观察一段时间的稳定值、尖峰和内存趋势。
- Deep Profile会增加测量开销，常规基线应关闭，需要深入函数时才短时开启。
- Profiler默认滚动保留最近300帧，旧帧会被覆盖；Preferences的Analysis/Profiler中可把Frame Count提高到最多2000，但会增加开销。

- 创建脚本或安装包不等于功能完成；还需要场景配置、运行验证和可复现结果。
- 性能结论必须来自Profiler数据，不能只说“使用对象池所以性能好”。
- 尚未实现或未验证的功能不能写入README和简历。
