# 《撤离测试场》知识记录

> 这里只记录“是什么、为什么、有什么区别”。具体点击、命令和实际结果统一记录在《操作与作用.md》。内容随开发推进补充，不提前把未接触的技术写成已掌握。

## 1. 项目定位与范围控制

- 项目类型：俯视角 3D 生存撤离 Gameplay Demo。
- 核心循环：进入地图 → 搜索物资 → 对抗敌人 → 维持生命/口渴 → 获得任务物品 → 到达撤离点结算。
- 选择俯视角的原因：降低第一人称动画、镜头和美术成本，把时间集中在输入、战斗、交互、AI、背包、存档和性能分析上。
- MVP 指“最小可玩版本”：玩家能完成一局核心循环，不等于所有美术、音效和扩展功能都完成。
- 范围越小，越容易留下完整、可验证、能在面试中独立解释的实现证据。

## 2. Unity 版本与 LTS

- LTS（长期支持版）以稳定和修复为主，适合确定版本后持续制作的项目。
- 新版本不等于项目质量更高。校招更关注 C#、数据结构、Gameplay 实现、调试和性能证据，而不是具体的 Unity 小版本号。
- 商业项目常因插件、平台和测试成本长期锁定某个 LTS，所以理解生命周期、API 和迁移方法比只会最新版更重要。
- 本项目使用 Unity 2022.3.57f1c1：本机已有环境，旧项目经验可迁移，资料和插件兼容性充足，适合 12 天交付。

## 3. 渲染管线

| 管线 | 特点 | 本项目判断 |
|---|---|---|
| Built-in | 传统管线，旧教程和旧素材兼容性好 | 可用，但不是本项目选择 |
| URP | 轻量、跨平台、现代工作流，画质和复杂度平衡 | 采用 |
| HDRP | 面向高端画质，配置和性能成本更高 | 超出范围 |

- URP 只负责渲染，不会自动提高 Gameplay 代码质量。
- 素材导入前要查看 Render Pipeline Compatibility 和 Unity Version。
- Built-in 素材使用的 Standard Shader 在 URP 中可能变成紫色；普通材质通常可转换，自定义 Shader 和老粒子效果不保证兼容。
- 只使用 URP 模板不能算简历亮点；真正可讲的是材质兼容、Draw Call、阴影或 Profiler 中实际验证过的问题。

## 4. 空白模板与 Sample 模板

- `Universal 3D` 是空白核心模板，只带开始 URP 项目所需的基本配置。
- `Universal 3D sample` 带完整演示场景、教程、示例控制器、特效和性能资源。
- Sample 适合学习管线功能，但不适合本项目起步：目录噪声大、体积大，也容易混淆“官方示例代码”和“本人实现代码”。
- 作品项目应让代码归属清晰，所以采用空白核心模板。

## 5. Unity 布局与项目的关系

- Unity 的窗口布局保存在用户 AppData 中，属于编辑器全局配置，不属于 Assets、Packages 或 ProjectSettings。
- 布局文件引用了当前项目不存在的 EditorWindow 时，会出现 `layout could not be fully loaded`。
- 这种警告通常不影响场景、脚本、编译或打包，应先区分“编辑器界面问题”和“项目代码问题”。
- Console 中旧日志不会自动消失，排查时要先 Clear，再观察是否重新产生。

## 6. Unity 项目目录与 Git

| 内容 | 是否提交 Git | 原因 |
|---|---:|---|
| Assets | 是 | 场景、脚本、资源及 `.meta` |
| Packages | 是 | 包依赖与锁定版本 |
| ProjectSettings | 是 | 项目和平台配置 |
| Library | 否 | Unity 可重新生成，体积很大 |
| Temp、Logs、UserSettings | 否 | 临时或本机状态 |
| `.sln`、`.csproj` | 否 | IDE/Unity 可重新生成 |

- `.meta` 保存资源 GUID 和导入设置，场景、Prefab 和脚本引用依赖 GUID，因此必须与资源一起提交。
- `.gitignore` 的作用是阻止生成文件进入版本库，不是删除已有文件。

## 7. Plastic 与 Git

- Unity Version Control（Plastic）和 Git 都是版本控制系统，但工作区元数据和操作流程不同。
- 同一路径被 Plastic 重复登记时，会出现“已包含在某个工作区中”。这属于版本控制配置冲突，不是 C# 或 URP 错误。
- 本项目使用 Git，便于展示提交历史、关联 GitHub，并符合常见校招作品交付方式。

Git 基本阶段：

1. `git init`：在目录中创建仓库，只做一次。
2. `git add .`：把当前改动加入暂存区。
3. `git commit`：生成一个本地版本快照。
4. `git push`：把本地提交上传到已配置的远程仓库。

`add` 不等于提交，`commit` 也不等于上传远程。

## 8. C# Debug 与 Release

- Unity 编辑器的 `Mode: Debug` 允许 C# 断点调试，但代码优化较少。
- Release 会启用更多 C# 优化，调试体验下降，切换时会重新编译脚本。
- 编辑器的 Debug/Release 与最终 Player 构建配置不是同一概念。
- 开发阶段保持 Debug；性能验证或正式构建时再明确记录测试条件。

## 9. 旧 Input Manager 与新 Input System

| 对比 | 旧 Input Manager | 新 Input System |
|---|---|---|
| 常用 API | `Input.GetKey`、`Input.GetAxis` | `InputAction`、`PlayerInput` |
| 配置 | Project Settings 中的轴和字符串 | `.inputactions` 资源、Action、Binding |
| 读取方式 | 主要在每帧轮询 | 支持轮询和事件回调 |
| 改键/手柄 | 需要较多自定义代码 | 原生支持绑定、控制方案和设备 |
| 耦合 | 玩法代码容易直接依赖具体按键 | 玩法只关心 Move、Fire 等意图 |

- Action 表示玩家意图，例如 Move、Aim、Fire；Binding 表示具体按键或设备控件。
- Action Map 用于组织一组输入。例如 Gameplay 与 UI 分开后，打开菜单时可以关闭 Gameplay 输入。
- `Both` 会同时启用旧版和新版输入，便于兼容旧项目，但容易出现两套 API 混用。
- 本项目选择 `Input System Package (New)`，代码不使用 `Input.GetKey` 或 `Input.GetAxis`。
- 计划的 Gameplay Action 包括 Move、Aim、Fire、Interact、UseItem、快捷栏和 Pause；只有真实建立后才记录为已完成。

## 10. 证据与简历边界

- “安装了包”只证明环境具备能力，不等于完成了功能。
- “使用了对象池/Profiler/状态机”等描述必须有真实代码、运行验证和证据路径。
- AI 辅助代码必须能说明输入、输出、生命周期、依赖、边界条件和修改方法，才能视为本人掌握。
- MVP、性能验证和最终完成三个节点分别整理成果摘要；未实现内容不能提前写入简历。

